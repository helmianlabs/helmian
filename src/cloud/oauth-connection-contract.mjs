import { createHash } from 'node:crypto';
import { normalizeActorRole, normalizeTenantId } from '../core/tenant-context.mjs';
const PROVIDERS = Object.freeze(['openai_codex', 'claude', 'gemini', 'grok']); const ADMIN = new Set(['owner', 'admin']); const STATE = /^[A-Za-z0-9_-]{32,128}$/; const PKCE = /^[A-Za-z0-9~._-]{43,128}$/; const SENSITIVE = /(token|secret|credential|password|private.?key|client_secret|code_verifier)/i;
function freeze(v) { if (v && typeof v === 'object' && !Object.isFrozen(v)) { for (const c of Object.values(v)) freeze(c); Object.freeze(v); } return v; }
function validScope(v) { return Array.isArray(v) && v.length > 0 && v.length <= 12 && v.every((x) => typeof x === 'string' && /^[a-zA-Z0-9:._/-]{1,96}$/.test(x)); }
function reject(input) { if (!input || typeof input !== 'object' || Array.isArray(input)) throw new TypeError('input'); for (const k of Object.keys(input)) if (SENSITIVE.test(k)) throw new TypeError('sensitive'); }
export function listCloudProviderConnectionReadiness(input) { try { reject(input); const tenant_id = normalizeTenantId(input.tenant_id), actor_role = normalizeActorRole(input.actor_role); if (!ADMIN.has(actor_role) || Object.keys(input).some((k) => !['tenant_id','actor_role'].includes(k))) throw new TypeError('scope'); return freeze({valid:true,result:{format:'helmion.cloud-provider-readiness.v1',tenant_id,actor_role,providers:PROVIDERS.map((provider_id)=>freeze({provider_id,status:'provider_registration_required',desktop_subscription_reusable:false,cloud_connection:'not_configured',refresh_token_storage:'external_encrypted_vault_required',invocation:'not_performed'}))}}); } catch { return freeze({valid:false,code:'CLOUD_PROVIDER_READINESS_INVALID'}); } }
export function createCloudOAuthAuthorization(input) { try { reject(input); const allowed=['tenant_id','actor_role','provider_id','state','code_challenge','code_challenge_method','redirect_uri','scopes']; if(Object.keys(input).some((k)=>!allowed.includes(k))) throw new TypeError('input'); const tenant_id=normalizeTenantId(input.tenant_id),actor_role=normalizeActorRole(input.actor_role); if(!ADMIN.has(actor_role)||!PROVIDERS.includes(input.provider_id)||!STATE.test(String(input.state))||!PKCE.test(String(input.code_challenge))||input.code_challenge_method!=='S256'||!validScope(input.scopes)) throw new TypeError('input'); const redirect=new URL(input.redirect_uri); if(redirect.protocol!=='https:'||redirect.username||redirect.password||redirect.hash) throw new TypeError('redirect'); return freeze({valid:true,result:{format:'helmion.cloud-oauth-authorization.v1',tenant_id,actor_role,provider_id:input.provider_id,state:input.state,code_challenge:input.code_challenge,code_challenge_method:'S256',redirect_uri:redirect.toString(),scopes:freeze([...new Set(input.scopes)].sort()),status:'authorization_pending',provider_endpoint:'not_configured',token_exchange:'not_performed',refresh_token_storage:'external_encrypted_vault_required',audit:'connection_intent_recorded'}}); } catch { return freeze({valid:false,code:'CLOUD_OAUTH_AUTHORIZATION_INVALID'}); } }
export function verifyCloudPkce({code_verifier,code_challenge}={}) { try { if(!PKCE.test(String(code_verifier))||!PKCE.test(String(code_challenge))) throw new TypeError('invalid'); return freeze({valid:createHash('sha256').update(code_verifier).digest('base64url')===code_challenge}); } catch { return freeze({valid:false}); } }
