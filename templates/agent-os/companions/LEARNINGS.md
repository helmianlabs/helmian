# LEARNINGS

<!-- Techniques, working methods, and discoveries worth reusing. Newest first.

     A learning answers "how do I do this well next time." A lesson answers
     "what must never happen again." When a method proves itself three times,
     consider promoting it into the core rules.

     Guidance lives inside comment blocks like this one. The session-start hook
     strips them, so only real entries reach the next session's context.

     Nothing leaves "Proposed" without the user approving it.

     Format:

     - [YYYY-MM-DD] One sentence naming the method and when it applies.
       Evidence: `file:line`, a command and its output, or a measurement.
-->

## Proposed

## Approved
