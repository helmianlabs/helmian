# LESSONS

<!-- Corrections that must not repeat. Newest first.

     A lesson earns its place by naming a trigger, a citation, and a checkable
     fact. If it has a moral but no number, it will not stop anyone — rewrite it
     until it would have stopped you.

     Guidance lives inside comment blocks like this one. The session-start hook
     strips them, so only real entries reach the next session's context.

     Nothing leaves "Proposed" without the user approving it.

     Format:

     ## YYYY-MM-DD — Short title

     **Trigger:** what you would observe that means this lesson applies.
     **What happened:** the actual failure, in one or two sentences.
     **The fact:** what is true, with `file:line`, a measurement, or a command.
     **Instead:** what to do when the trigger fires.
-->

## Proposed

## Approved
