# BLOCKERS

<!-- What is stuck right now. The session-start hook reads this file, so
     anything under "Open" is the first thing the next session learns. Keep
     guidance inside comments like this one — the hook strips comment blocks,
     so only real entries reach the next session's context.

     This file holds OPEN blockers only. When one is resolved, delete it — and
     if it taught you something, write that up in LESSONS.md first. A file that
     also accumulates resolved items grows until nobody reads the top of it.

     Add new entries anywhere; the hook reads the whole file.

     Format:

     ## Short title of what is blocked

     **Since:** YYYY-MM-DD
     **What is blocked:** the work that cannot proceed.
     **What was tried:** each attempt and how it failed, with the real error.
     **What would unblock it:** the specific missing thing — a credential, a
     decision, an answer, an access grant.
     **Owner:** who has to act (you, a vendor, another team, the agent).
-->

## Open
