# SESSION BOARD

<!-- Live registry of agent sessions. Add a row when you start, set it to `done`
     when you finish, and claim your files BEFORE you edit them.

     If a file you need is claimed by an `active` row, do not touch it. Ask, or
     work somewhere else. Two sessions writing one file produces a broken file
     and two confident reports that everything went fine.

     The session-start hook shows only rows whose status is `active`, so stale
     `done` rows cost nothing — but delete them occasionally anyway.

     This matters most when several agents run at once, and costs nothing when
     only one does.

     Example row:
     | session-a | fix login redirect | src/auth/session.ts | 2026-01-01 09:00 | active |
-->

| session | scope | claimed files | started | status |
|---|---|---|---|---|
