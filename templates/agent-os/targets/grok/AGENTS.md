# Agent OS

Installed by `helmion agent-os install`. This is the global rules file: it is
read at session start, before any repository's own `AGENTS.md`, and a file
deeper in the tree wins where the two disagree. Edit it freely — it is yours
now, and the loop below is how it gets better.

{{CORE_RULES}}

{{LOOP_SUMMARY}}
