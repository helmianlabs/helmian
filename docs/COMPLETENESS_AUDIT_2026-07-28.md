# COMPLETENESS AUDIT — 2026-07-28

**Session:** `f3673e34/agent-N-completeness-audit` · READ-ONLY · no code changed.
**Question answered:** of everything Troy asked for today, what is genuinely finished
end to end, and what only looks finished?

---

## GATES THIS DOCUMENT WAS WRITTEN UNDER (quoted verbatim)

> **Rule 0.001 — SOURCE OF TRUTH + FULL TRACING.** Every factual claim must satisfy
> BOTH gates. **GATE 1 — SOURCE OF TRUTH.** Cite the primary source where the claim
> lives. Code → file:line. NOT "memory says," NOT "the handoff says." **GATE 2 — FULL
> TRACING.** For any multi-stage claim, draw the chain explicitly with a citation on
> EVERY link. Stage A → B → C → ... → N. **A chain with one uncited link IS the lie.
> The gap with no citation IS the gap.**

> **Rule 0.002 — CITE OR SHUT UP.** If I cannot cite a primary source for a claim
> right now, I do not make the claim.

> **Rule 0.13 — RETIRED 2026-05-29.** No 45/60-minute handoff cap. Discipline, not a
> clock.

**Method.** Every verdict below was traced against source code on disk. No verdict was
inherited from `HANDOFF_2026-07-28_EVENING.md`, `GEMINI_DOC_VERIFICATION_2026-07-28.md`,
`FLYWHEEL_AUDIT_2026-07-28.md`, `MOSHI_INSTALL_ATTEMPT_2026-07-28.md`,
`VOICE_STACK_ASSESSMENT.md`, or `VOICE_PHASE1_WIRING_TODO.md` — those were read only to
learn what was *claimed*. Two claims made by other agents today are **refuted** below
with citations. Bodies were read, not just names: `ToolDispatcher.cs` has real handlers
and zero live callers, and the same pattern was found three more times.

---

## SEVERITY-RANKED FINDINGS

### 🔴 TIER 1 — DATA ALREADY LOST, OR A SECURITY HOLE THAT IS LIVE RIGHT NOW

| # | Troy asked for | Actual state (file:line / probe) | Verdict | What remains |
|---|---|---|---|---|
| **22 / d** | Rules + learnings files kept intact | **Troy's `BASE_RULES.md` was destroyed and is NOT recovered.** On disk now: 1,635 B, **byte-identical to `ClaudeProfileInstaller.cs:185 BaseRulesTemplate`**. Prior state recorded four times in `planning/STATE.md:156,183,232,278` as 5,512 B / 105 lines. `~/.claude/LEARNINGS.md` = 361 B, byte-identical to `ClaudeProfileInstaller.cs:218 LearningsTemplate`. Writer: `ProfileSyncEngine.cs:196-208` approved-set → `ClaudeProfileInstaller.cs:97 File.WriteAllTextAsync`. Fired **15:34:58**; the `overwriteExisting=false` default + `.bak` logic were added at **16:05 — 31 minutes too late**. `_snapshots/2026-07-28_161500/` preserves both at 15:34:58. No surviving pre-truncation copy anywhere on `.claude`, `_snapshots`, `E:\Helmion`, or OneDrive. | **🔴 DATA LOST — UNRECOVERED** | ~3,877 B of `BASE_RULES.md` and all of Troy's `LEARNINGS.md` can only come from a backup **only Troy can reach**. Then delete lines 200-201 from `ProfileSyncEngine.cs` so no future caller can target them. `LESSONS.md` recovered on its own (7,799 B) only because `distill.mjs:36` re-appends from Neon. |
| **b** | (unruled) CLI tool defaults | `src/agent/session.mjs:89` — `let permissionMode = 'full';`. `tools.mjs:135` — `if (permissionMode === 'full') return true;` unconditionally, including `run_command` (`tools.mjs:327`, "Run a shell command… PowerShell on Windows"). Approver is `undefined` in full mode (`session.mjs:149`); the approval gate at `tools.mjs:428` only fires for `ask`; control reaches `tools.mjs:437 await tool.execute()` directly. **Desktop, same codebase, starts at read-only** (`MainWindow.xaml:1261 SelectedIndex="0"` → `Tag="read-only"` at `:1264`). | **🔴 STILL OPEN — TROY HAS NOT RULED** | A fresh `helmion agent` runs arbitrary shell with no prompt, no allowlist, no confirmation. Unchanged per instruction. Troy's call: match the desktop's read-only default, or keep full and document it. |
| **1** | Secret redaction, **both** layers | Both layers are **real** — a prior claim that only tool results are redacted would be wrong. Tool layer: `tools.mjs:443` `return redactSecrets(String(result))` catches every tool; into history at `loop.mjs:134`. Outbound layer: `providers.mjs:21-27 redactOutboundBody` applied at all three fetches (`:138` OpenAI/xAI/custom, `:227` Anthropic, `:319` Gemini) — grep proves those are the only `fetch` calls in `src/`. **The defect is the pattern set and the C# side.** Measured, not reasoned — 18 inputs executed: `postgres://` **NOT redacted** because `redact.mjs:9` reads `postgresql?:` and the `?` binds to the `l`. Also missed: `sk_live_` (Stripe), `AKIA` (AWS), `xoxb-` (Slack), `hf_`, JWT `eyJ`, `-----BEGIN RSA PRIVATE KEY-----`, `mysql://`, bare 32-hex, and **any generic `KEY=value` .env line**. C# has **zero** redaction: `ToolDispatcher.cs:152` returns raw file bytes, `:114-120` raw shell output. | **🟠 PARTIAL** | Fix `redact.mjs:9` (`postgres(ql)?://`) and add generic `KEY=<value>`, private-key, JWT, AWS, Slack, Stripe rules. The un-redacted C# stack is currently unreachable (see Tier 2, item 1b) — delete it or port `redactSecrets` to C#. |
| **a** | (known bug) commit-QA hook `-F` regex | `hook_block_commit_qa.ps1:117` — `'(?:^\|\s)(?:-F\|--file)\s+(?!-\s)([^\s"'']+)'`. The class `[^\s"']+` forbids quotes, so a quoted path cannot satisfy the `+`. **Tested in isolation, 6 forms:** unquoted MATCHES; `-F "path with space"`, `-F "path"`, `-F 'path'`, `--file="path"` all **NO-MATCH**. Line 118's `.Trim('"',"'")` is dead code. **Two failure modes, opposite directions:** quoted path → `$msg=""` → `:158 $qaFound=$false` → **`:178 exit 2`, a good commit is BLOCKED**; and `git commit -F -` captures `-` (lookahead `(?!-\s)` misses `-` at end of string) → `$unresolvedFileArg=$true` → **`:149 exit 0`, a commit with ZERO QA evidence is ALLOWED.** | **🔴 STILL OPEN (both directions)** | One-line replacement, validated on all 7 forms: `'(?:^\|\s)(?:-F\|--file)(?:\s*=\|\s+)\s*(?!-[\s;\|&]\|-$)(?:"(?<p>[^"]+)"\|''(?<p>[^'']+)''\|(?<p>[^\s"'']+))'` plus `$p = $m.Groups['p'].Value` at `:118`. |

---

### 🟠 TIER 2 — BUILT, TESTED, GREEN — AND NO HUMAN CAN REACH IT

Everything in this tier compiles and passes tests. That is exactly why it is dangerous:
each one reads as "done" on a board.

| # | Troy asked for | Actual state (file:line / probe) | Verdict | What remains |
|---|---|---|---|---|
| **7** | Slash commands + plugins on the **desktop** path | Engine is real and complete (`commands.mjs:245,303,422,498`). CLI works (`session.mjs:370-398`). Bridge exposes `commands` (`bridge.mjs:276`), `expand` (`:295`), and even auto-expands a leading `/` inside `turn` (`:308-314`). **The desktop reaches none of it — and worse, hijacks it.** `MainWindow.xaml.cs:1487-1488`: comment *"Explicit local shell escape (does not go through the agent API)"*, then every `/`-prefixed input except `/reset` goes to `:1505 _consoleSession.SendAsync(text.Substring(1))` — **a PowerShell child process** (`ConsoleSession.cs:265` writes to its stdin). Typing `/deploy staging` in the Pilot **runs `deploy staging` as a shell command.** Complete list of what `AgentBridge.cs` can send: `hello:123`, `turn:150`, `configure:180`, `permission_response:215`, `reset:225`. No `commands`, no `expand`. Plugin UI: grep for `plugin` across all desktop `.cs`/`.xaml` → **zero hits**. | **🔴 DONE-BUT-HIJACKED** | Narrow the `/` escape at `MainWindow.xaml.cs:1488` to a reserved prefix (e.g. `!`) so slash text falls through to `TurnAsync`, which the bridge already expands. |
| **5** | Per-task model auto-switcher, **incl. desktop UI surfacing the chosen model** | Engine **DONE-VERIFIED**: 3 tiers (`model-router.mjs:21`), triggers cited `:232-266`, chain to the wire complete (`loop.mjs:72` → `:89-97` → `providers.mjs:117-119` → `:138`). Node **does** emit the choice: `loop.mjs:73-81` `{type:'model',…}` → `bridge.mjs:361-370` `{"event":"model"}`. **Then it dies three times.** (1) `AgentBridge.cs:313-349 ParseEvent` extracts 15 fields — no `model`, no `tier`. (2) `AgentBridgeEvent` (`AgentBridge.cs:498-515`) has 16 properties, **none named Model or Tier** — verified directly. (3) `grep 'case "model"' MainWindow.xaml.cs` → **zero hits**, verified directly; the event falls through the switch at `:1552-1627` and is discarded. `MainWindow.xaml` grep for model/tier → 1 hit, `:2416 "MODEL NAME"`, the *custom-provider* field. **The CLI prints it** (`session.mjs:427-428`). | **🟠 PARTIAL — engine done, Troy's half NOT STARTED** | Add `Model`/`Tier`/`Reason` to `AgentBridgeEvent` + `ParseEvent`, add `case "model":` to the MainWindow switch, render a label in the console toolbar. |
| **3** | Honest status labels — nothing claiming live from config existence | **The thing Troy literally asked is FIXED**: no indicator claims live from a non-empty key. `PilotSnapshot.cs:152,162` say `"SAVED · UNVERIFIED"` with `IsLive:false`; custom providers are marked verified only after a real HTTP probe (`CustomChatSession.cs:112-129`). **But two worse categories survive.** (i) Claims LIVE from *nothing at all*: `MainWindow.xaml:730` hardcodes `Text="LOCAL LIVE"` beside a hardcoded green `Ellipse` at `:729` — **verified directly**; its only setter (`MainWindow.xaml.cs:1339`) also writes `"LOCAL LIVE"` — verified directly. Same for `ConsoleLiveLabel` (`xaml:1359`, driven by the *full-screen toggle*, `.cs:494`). Unfalsifiable literals: `"ORCHESTRATION ACTIVE"` `:1558` (stream always empty, `PilotSnapshot.cs:247`), `"LIVE TIMELINE"` `:1623` (Handoffs always empty, `:245`), plus 8 more. `MainWindow.xaml:2584` reads **"No network connections · No credentials loaded"** — flatly false. (ii) Claims connected **after the probe failed**: `.cs:1279` calls `ApplyWorkspaceInspection` unconditionally, and the `if (usedFallback)` repair at `:1280-1290` **misses `:1339`, `:1344`, `:1348`, `:1350`** — so with the service down the Overview reads **"Live Connected"** in green while `PilotSnapshot` on the same screen says `"NOT LIVE"`. | **🟠 PARTIAL** | Delete/bind the three hardcoded green claims; move `.cs:1339/1344/1348/1350` into the non-fallback branch; fix the false footer at `xaml:2584`. |
| **12** | **DICTATE MODE** — caret insertion + 4 spoken commands | `DictationCommands.cs` implements all four and then some: `"new line"` `:61`, `"scratch that"` `:66`, `"send it"` `:70`, `"stop dictation"` `:76`, with synonyms and punctuation-tolerant normalization `:113-123`. **Every reference in the repo:** the file itself, `VoiceSmokeChecks.cs:32,41,45-80,222-223` (20 unit assertions), and two prose lines in `VOICE_PHASE1_WIRING_TODO.md`. **Zero production callers** — I verified `grep DictationCommands MainWindow.xaml.cs` → zero hits, directly. No caret insertion from a transcript exists: the only `CaretIndex`/`SelectionStart` writes are auto-scroll `:242`, paste `:1447-1458`, restore-after-failure `:1476`. The live voice path (`:1714-1758`) sends the transcript **straight to the model** at `:1747`, never to the caret. `grep dictat MainWindow.xaml` → no matches. | **🟠 DONE-BUT-UNWIRED (as predicted — proven)** | Add a toggle beside `ConsoleMicButton` (`MainWindow.xaml:1480`); branch after the `_voiceTurnBusy` guard at `.cs:1725` to `DictationCommands.Detect(heard)`; splice `Literal` into `ConsoleInputBox` at `CaretIndex` using the span-concat already proven at `:1454-1458`. |
| **16b** | Device watchdog | **Output side is real**: `KokoroSpeechSynthesizer.cs:634-653` registers a live `IMMNotificationClient`; rebuilds on the next utterance at `:441-445`. **Input side has no watchdog at all**, and its only recovery path is **broken**: `WhisperSpeechRecognizer.cs:549-557 OnRecordingStopped` raises `Stopped` but never calls `Stop()` and never nulls `_capture` (only `SafeDisposeCapture:559-567` does, reached from `:180`/`:147`). So the restart at `VoiceSession.cs:375-398` lands on `Start():99-105`, which sees `_capture is not null`, flips `_running = true`, and **returns without reopening the device**. `IsRunning:80-83` then reports true → `LocalVoiceEngine.cs:100` sets `VoiceState.Listening`. | **🔴 PARTIAL — dead mic reports "Listening"** | Call `SafeDisposeCapture()` from `WhisperSpeechRecognizer.cs:551` when `e.Exception is not null`, so the next `Start()` falls through to `:113-138` and actually reopens. |
| **2b** | Custom endpoint key handling | The feature itself is **DONE-VERIFIED end to end** (see Tier 3). The key is not: `CustomProviderProfile.ApiKey` is serialized as **plain JSON** (`DesktopSettings.cs:133-136`) to `%LOCALAPPDATA%\Helmion\desktop-settings.json` (`:81-85`), and typed into an **unmasked `TextBox`** (`MainWindow.xaml:2427`) while built-in keys use a `PasswordBox` (`.cs:682-686`). A DPAPI store exists but is used only by smoke tests (`ProtectedProviderProfileStore.cs:43`; sole caller `SmokeTests/Program.cs:273`). | **🟠 PARTIAL** | Swap `CustomModelApiKeyInput` to a `PasswordBox` and route the key through the already-built `ProtectedProviderProfileStore`. |
| **1b** | (found during audit) Un-redacted second agent stack | `ToolDispatcher.cs` + the five `*ChatSession.cs` files form a **complete second agent stack** with a prompt telling the model to emit `[CMD: ReadWorkspaceFile …]` — and **zero redaction on either layer**. It is currently unreachable: `ToolDispatcher` is constructed only in `SmokeTests/Program.cs:567` and `AskPermissionSmokeChecks.cs:98`; `ConsoleSession.SendChatAsync` (`:168`) has one caller, `SmokeTests/Program.cs:473`; the shipping UI uses the Node bridge (`MainWindow.xaml.cs:1545`). **One `SendChatAsync` call site would light it up with no redaction.** *(This is the same shape the parent flagged: real handlers, dead code, zero live callers.)* | **🟡 LATENT** | Delete it, or gate it behind a C# port of `redactSecrets` before anyone wires it up. |

---

### 🟡 TIER 3 — GENUINELY DONE, VERIFIED END TO END

Each of these was traced with a citation on every arrow. No uncited gaps.

| # | Troy asked for | Proof of the full chain | Verdict |
|---|---|---|---|
| **6** | Ask-permission 4th tier, **WPF UI reachable and wired** | 10-link chain, every arrow cited: `tools.mjs:429` → `approval.mjs:141,168-177` emits `permission_request` → `bridge.mjs:66-68` NDJSON → **`AgentBridge.cs:341,342,343,347` parses id/tool/summary/timeout** → `:302` → `TurnAsync:164` → **`MainWindow.xaml.cs:1563 case "permission_request"`** → `:168 ConsoleApprovalPanel.Visibility = Visible` → buttons `MainWindow.xaml:1440/1448/1456` → `.cs:225-232` → `CompleteApproval:216-223` → `:1576 RespondToPermissionAsync` → `AgentBridge.cs:204-220` writes straight to stdin (bypassing the queue because the turn is in flight, `:196-201`) → **`bridge.mjs:417-427` intercepts out-of-band** → `approval.mjs:188-194` settles → `tools.mjs:429-437` acts. Selectable by a human at **`MainWindow.xaml:1268`** `Tag="ask"`. Fail-closed proven for: no approver (`approval.mjs:223-226`), timeout (`:239-242`), throw (`:234-237`), unknown id, shutdown, non-TTY. | **✅ DONE-VERIFIED** |
| **10** | Stale `XAI_API_KEY` mid-session fix | `bridge.mjs:158-176 refreshProviderKeyFromEnv`. Chain: `.env` edited → `:161 loadHelmionEnv` → `env.mjs:134-140` **force-overrides** inherited vars for keys in `HELMION_ENV_OVERRIDE_KEYS` (`XAI_API_KEY` at `env.mjs:15`) → `:166` compares old vs new → `:167` swaps → `bridge.mjs:353-357` → `loop.mjs:90` → **`providers.mjs:141 Authorization: Bearer ${apiKey}`** on the `:138` fetch. Called every turn (`bridge.mjs:350`). **History survives** — it never calls `reconfigure()`/`resetSessionState` (`loop.mjs:234-236` would wipe `state.messages`); the reason is written at `bridge.mjs:152-157`. Covers all four built-ins, not just xAI. `test/bridge-env-refresh.test.mjs` is a **real end-to-end test** — live HTTP server recording the `Authorization` header, asserts `Bearer stale-key-001` on turn 1 and **`Bearer fixed-key-002`** on turn 2 (`:156`,`:167-171`) and that history grew (`:179-185`). | **✅ DONE-VERIFIED (bridge)** · 🔴 CLI has no equivalent — `session.mjs:41` loads env once, `:70` resolves once; a key fixed mid-REPL keeps 401-ing until restart |
| **2** | Custom OpenAI-compatible endpoints — desktop AND CLI AND bridge | All three real. CLI `session.mjs:508-513` → `env.mjs:169-186`. Bridge `bridge.mjs:139-146` adopt → `:180 resolveProvider`. **Desktop UI exists and is reachable**: card at `MainWindow.xaml:2386`, inputs `:2417/2422/2427`, `AddCustomModelButton:2432` → `.cs:845-897`, real probe button `:2437` → `CustomChatSession.ProbeAsync` (POSTs, requires a `choices` array back). **Selectable as Maestro** at `MainWindow.xaml:2347`, custom names appended `.cs:797-801`. Outbound traced with every arrow: `.cs:1527` → `:1545` → `AgentBridge.cs:150-155` → `:457-469` → `bridge.mjs:332` → `:351` → `:180` → `env.mjs:177-181` → `loop.mjs:89-96` → `providers.mjs:61` → **`:138 fetch(url)`**. Keyless local runtimes handled (`env.mjs:177`). | **✅ DONE-VERIFIED** (key storage is Tier 2) |
| **11** | Phase 1 Whisper STT + Kokoro TTS, nothing regressed | SAPI gone — grep for `SpVoice\|SAPI\|System.Speech\|SpeechSynthesizer` across `desktop/` returns **zero code references** (only removal comments). Mic→Whisper: `WhisperSpeechRecognizer.cs:115-120` (16 kHz mono) → VAD `:381-451` → `:332-337` model load → `:273 ProcessAsync` → `:536` event → `MainWindow.xaml.cs:1744` on screen. Text→Kokoro: `:1637` → `SpeechTextCleaner:14-52` → `KokoroSpeechSynthesizer.cs:228 Infer` → `:449-450` **24 kHz / mono / 16-bit** → `:379-380` play. **Models present and byte-exact**: `ggml-base.en.bin` 147,964,211 B = expected `get-voice-models.ps1:49`; `kokoro.onnx` 325,508,342 B = expected `:55`; 54 voice `.npy` + espeak DLLs present. Mic button `MainWindow.xaml:1480`. | **✅ DONE-VERIFIED** (nits: stale `bin/Release/.../System.Speech.dll` build debris; `VoiceModelPaths.cs:102-106` accepts a `models` dir on existence alone) |
| **16a** | Persistent mic toggle | `VoiceSession.cs:13 _voiceMode`; the only writes are `:73` (start), `:85/:97` (failure), `:110` (user stop), `:309` (dispose). **Nothing clears it at end of turn** — the per-turn flag is the separate `_pausedForTts` (`:140`/`:161`). Underneath, `WhisperSpeechRecognizer.Pause():160-168` keeps the device **open**, and `ResumeListeningAfterTts` unconditionally restarts (`:166-172`, with `:164-165` recording that an earlier early-return version left the mic dead). | **✅ DONE-VERIFIED** (the button's *look* is set imperatively at `.cs:1688/1699/1704`, not bound to `IsListening`) |
| **16d** | Text always renders | `MainWindow.xaml.cs:1608-1613 case "assistant"` → `AppendConsoleLine(ev.Text)` is **unconditional**, no voice check in the branch. Only *after* rendering does `:1612` fill the `spoken` buffer, and the TTS branch at **`:1632`** (`if (_voiceSession is { IsVoiceModeActive: true } …)`) is strictly downstream. A TTS throw is caught at `:1639` and does not unwind the transcript. `SpeakAsync` itself returns early rather than throwing when no output device exists (`VoiceSession.cs:207-213`). | **✅ DONE-VERIFIED** |
| **4** | Console input clears on submit, **all paths** | Exactly three submit paths exist (grep for `SendConsoleInputAsync` — no command palette exists in the desktop). Enter `.cs:1397-1412`→`:1411`; Send button `:1389-1392`; voice `:1714`→`:1747`. **All three clear at `:1484`** — verified directly — which sits *before* the `/` branch at `:1488` and the agent branch at `:1513`. Deliberate non-clears are correct and self-documented: `:1466-1470` (busy, nothing dispatched), `:1472-1480` (text explicitly restored, reason at `:1474`), `:1670-1671` ("anything typed during the turn is the user's next message and must survive"). | **✅ DONE-VERIFIED** (one nit: `:1484` runs even when `overrideText` came from voice, silently destroying a typed draft) |
| **17** | LESSONS topic split + pointer-only start + UserPromptSubmit hook — **verify the hook fires** | 11 topic files + `INDEX.md` under `~/.claude/lessons/`. Session start is pointer-only: `session_start_inject.sh:39-44` awk-extracts only the `ROUTES` rows; lessons block is **1,189 B of pointers**, down from a 3,832 B raw dump. **Hook registered — I verified `settings.json:125` directly**, 4th `UserPromptSubmit` entry. **Hook has fired LIVE in production**: `~/.claude/_topic_lessons_state.json`, 82 B, mtime **2026-07-28 19:53:46**, contents `{"pc":"lessons-neon.md","f3673e34-…":"lessons-voice.md"}` — **that second key is this very session's ID**. Exercised independently under a redirected `USERPROFILE` (real state byte- and mtime-identical afterward): a git-topic prompt emitted valid `hookSpecificOutput` JSON carrying `lessons-git.md`, exit 0. Safeguards: `:81` non-alphanumeric boundary (so `auth` won't fire on `author`), `:91` caps at 2 files, `:112` refuses any file >20,000 B, every path exits 0. All 8 LESSONS entries routed exactly once (verified 1:1 by `SOURCE: blocker #` tags). | **✅ DONE-VERIFIED — hook proven firing live** |
| **19** | Tier-B consensus gate FK fix | The FK is `sql/001_helmion.sql:83` — `advisory_reviews.action_hash REFERENCES governance_actions(action_hash)`. Nothing ever inserted the parent, so every `recordReview` hit Postgres 23503 and rolled back. Fix = the **missing writer**: `neon.mjs:451-469 registerGovernanceAction` + `insertGovernanceAction:181-206` (idempotent `ON CONFLICT DO NOTHING` + read-back); `recordReview:479-493` now checks for the parent. The hash is derived from canonical JSON (`advisory-action.mjs:47-52`), never caller-supplied — an agent cannot manufacture its own approval. **No migration needed**: `governance_actions` already exists at `sql/001_helmion.sql:69`. Test parses the real `sql/` + `src/` tree and uses a stub raising real 23503. **25 tests pass, 0 fail.** | **✅ DONE-VERIFIED** |
| **20** | Rule 0.28 session-end detail writer | `session_end_detail.mjs:63-78` emits `{outcome, cwd, project, host, ended_at, node, platform, git, branch, head, dirty_files, commits_today, citation, lesson}`; never throws (`:79-81`), never reads stdin. `session_end_safe.sh:38` calls it, `:41` passes it to `bigsister_neon_log.mjs event … session_ended`, `:43` falls back detail-less. **Registered — I verified `settings.json:164` directly**, `SessionEnd`, 15 s timeout. `JSON.parse` guard: `bigsister_neon_log.mjs:60-67` throws `MalformedDetailError` **before any write** (pre-fix behavior was exit 0 with no row). | **✅ DONE-VERIFIED** |
| **9** | agent-os install package | `agent-os.mjs:346/401` writes marker-delimited `CLAUDE.md`/`AGENTS.md`/`GEMINI.md` (append, never replace, `:314-326`), 6 companions (skip-if-present, `:231-233`), and managed `agent-os/` files. Never writes a settings file (`:393 hooksWired:false`, pinned by `test/agent-os.test.mjs:166-182`). **All 20 referenced templates exist on disk; zero missing.** `bin/helmion.mjs:243`; `package.json:19` includes `"templates"` in `files`. **Anonymization CLEAN — zero hits** across all 20 files for troyh/Troy/E:\Helmion/C:\Users/bigsister/neondb/DairyForge/AimForge/Caldmere/keys; the one naive substring hit (`CORE_RULES.md:99` "de**stroy**") is correctly excluded by the `\btroy\b` boundary and pinned as a non-match at `test/agent-os.test.mjs:272`. Scrubber has a real positive control: 11 poisoned fixtures, all flagged (`:240-263`). Hooks genuinely work, proven by **real child-process execution** (`:308`,`:377-400`,`:405-422`). | **✅ DONE-VERIFIED (package)** · ⚠️ **never installed anywhere real** — `~/.claude/agent-os`, `~/.codex/agent-os`, `~/.gemini/agent-os` all absent; every test uses `mkdtemp`. Also: `stop-capture.mjs` writes a journal **nothing ever reads** (`LOOP.md:17` is prose, not code) |
| **18** | Advisory read path `list\|show\|promote\|reject` | `bin/helmion.mjs:284` defines it, `:292` enforces the four verbs, `:697` dispatches, `:296` uses `BIGSISTER_DATABASE_URL`. Gate is strong: `advisory-lane.mjs:58-83` requires a named reviewer, a ≥20-char note, and an exact typed `PROMOTE <id>`/`REJECT <id>`; `helmion.mjs:372` refuses without a TTY. **`promote` does not move a row** — `advisory-lane.mjs:157-167` is an `UPDATE … SET review_decision/promoted` on the same low-trust row plus an `agent_logs` attribution row (`:247-266`). Nothing is copied into `decisions`/`context`/`sprints`. Correct under Rule 0.27; the verb name overstates it. **The verbs do NOT die without the DDL** — `laneCapabilities:105-112` detects the missing column and falls back to the legacy `promoted` boolean. | **🟡 PARTIAL** — two real degradations: `list --state rejected` throws (`:130-134`), and **`reject` silently no-ops on the row** (`:172-176` sets `promoted=false` where it already is), so a rejected row reappears in `unreviewed` forever |

---

### 🔵 TIER 4 — NOT STARTED / BLOCKED (reported plainly, nothing fixed)

| # | Troy asked for | Evidence | Verdict | Concrete first step |
|---|---|---|---|---|
| **13** | Phase 2 local voice host `ws://127.0.0.1:8765`, `/whisper` `/kokoro` `/health`, offline pill | Repo-wide grep for `8765` → **zero occurrences in code**. Every hit for `ws://`/`WebSocket`/`Voice host` is plan text, a comment, a *detector* regex (`mcp-code-audit.mjs:268`), or a download URL. `MoshiAvailabilityProbe.cs:42` uses port **8998**, not 8765. | **NOT-STARTED** (confirmed) | Everything. Nothing partial exists. |
| **15** | Barge-in | `VoiceSession.cs:342-351 Engine_SpeechDetected` exists but **can never fire during TTS**: `:228 PauseListeningForTts()` → `:143 _engine.PauseDictation()` → `LocalVoiceEngine.cs:114` → `WhisperSpeechRecognizer.cs:160-168 _suspended=true` → **frames dropped at the door, `:365`** — and `SpeechDetected` is only raised past that gate (`:415`). Resume happens only after Speak returns (`VoiceSession.cs:268-279`). Two further guards (`:327`, `:333`) would also drop it. **The test suite pins the dead behavior**: `VoiceSmokeChecks.cs:124-125` asserts the mic *was* paused for the whole playback; `RaiseSpeechDetected` (`:324`) is never called. | **DEAD** (proven) | Four changes: gate `VoiceSession.cs:228` behind a barge-in flag; relax `:327` and `:333`; **add echo cancellation** — `WhisperSpeechRecognizer.cs:115-120` uses bare `WaveInEvent` with a pure-energy VAD (`:386-388`), so a hot mic will interrupt itself on Kokoro's own output; rewrite `VoiceSmokeChecks.cs:124-125`. The cancellation half is already built (`StopSpeaking:282-294` → `CancelSpeak:321-332`). |
| **14** | Phase 3 Moshi | `VoiceBackendSelector.cs` **exists** (445 lines), selects `VoiceSession` vs `IDuplexVoiceSession` (`:34-35`), Moshi-preferred (`:60`), full fallback chain (`:134-158`, `:361-406`). **No fake adapter**: grep `MoshiDuplexSession` → **zero hits**; the only `IDuplexVoiceSession` implementor is `FakeDuplex` inside the test file. `MoshiAvailabilityProbe.cs:69-70` opens a **real `TcpClient`**, not a hardcoded value. **Selector NOT wired** — I verified directly: zero `VoiceBackendSelector` hits in `MainWindow.xaml.cs`, and **`:594 _voiceSession = new VoiceSession();`** constructs it directly. Moshi doc carries **verbatim pasted** CUDA output (`:129-136`, "Tried to allocate 14.32 GiB. GPU 0 has a total capacity of 6.00 GiB") and self-flags its own unmeasured claims at `:153-166`. | **BLOCKED (honestly)** | Nothing on Moshi until a ≥12 GB GPU exists. ⚠️ **`VoiceBackendSelector.cs.fixed.bak` (18,098 B) is a parked, materially different implementation** with an `_epoch` atomic pair-writer; the live `.cs` is byte-identical to `.control.bak`, i.e. the **unfixed** version. Someone's A/B is stranded mid-flight — resolve it. |
| **16c** | `VoiceState.Degraded` | Exists (`ISpeechEngine.cs:23`), set correctly at `LocalVoiceEngine.cs:63,100,221,256`, surfaced via `StateChanged:239` and consumed by `VoiceBackend.cs:57-58` ("Voice degraded — text only"). **The UI does nothing with it** — grep of `desktop/Helmion.Desktop` for `VoiceState\|Degraded\|EngineState` → 1 hit, an unrelated comment at `MainWindow.xaml.cs:1729`. The pill lives only in the selector the app never constructs. | **PARTIAL** | Subscribe to `StateChanged` and render a pill; the state machine underneath is already correct. |
| **8** | MCP install security pipeline — reachable from the desktop? | Pipeline is real and rigorous (`mcp-install-pipeline.mjs:91 runInstallPipeline`, 5 stages). CLI wired at `bin/helmion.mjs:534`/`:695`. **Desktop grep for `mcp\|pipeline\|discovery\|ledger` finds hits in exactly two places, neither the pipeline**: `ProfileSyncEngine.cs:106-179` (Helmion registering *its own* servers into other clients' configs) and two display strings. `AgentBridge.cs:64` launches only `agent-bridge`. **Architecturally undrivable**: `mcp-approval.mjs:80-89 assertInteractiveTerminal` throws unless `stdin.isTTY && stdout.isTTY`, called unconditionally at `:201`, then `readline.question` at `:208` — **a WPF app has no TTY on the Node child's stdin.** | **CLI-ONLY** | Not a button — needs a new approval transport. `mcp-approval.mjs:224-228` already names the right one (`src/core/human-confirmation.mjs`). Also: the live Neon ledger write (`mcp-install-ledger.mjs:293-295`) is proven **only against injected fakes** (`test/mcp-install-security.test.mjs:561,587`); every other call passes `connectionString:null`. |
| **21** | bigsister review-state DDL | `E:\Helmion\sql\` contains exactly three files; **`sql/bigsister/` does not exist**. Referenced as missing by `advisory-lane.mjs:101,132` and `helmion.mjs:336`. The gate is real and fired live during this audit — a read-only grep for DDL keywords was **BLOCKED** by `hook_autonomy_boundary.ps1:205-206` (`create\s+table\|alter\s+table\|…` → "contains a schema change (DDL)"), approval route `TROY-APPROVED:` + ≥20 chars (`:190`). | **BLOCKED — still awaiting Troy (agent_logs#394)** | Troy approves and runs the DDL. Side finding: the hook's skeleton stripper does not strip **double-quoted** strings, so a read-only `grep "create table"` false-fires. |
| **c** | `~/.claude` under version control | `git -C C:\Users\troyh\.claude rev-parse` → *"not a git repository (or any of the parent directories)"*. No parent repo. **Not OneDrive-synced** — `C:\Users\troyh\OneDrive\.claude` does not exist; `.claude` is a plain local dir with no reparse point. 23 scattered `.bak` files are the de-facto versioning. At risk: `CLAUDE.md` 35,900 B, `settings.json` 6,395 B, `skills/` 89 files, `memory/` 115 files. | **STILL OPEN** | **Not one command** — `git add -A` would commit live secrets. `.gitignore` FIRST for: `.credentials.json` (17,756 B OAuth), `_google_key.txt`, `projects/` (2.9 GB, contains live-key patterns in at least 4 memory/handoff files), `paste-cache/`, plus ~4 GB of `plugins/`, `_voice/`, `*.apk`, `history.jsonl`. Then `git init && git add -A && git commit`. |
| **23** | Driver app: Cora off Hume, **keep** Hume for Gauge's 60 s clips | **NOT-STARTED** — and **the premise is wrong.** Cora's STT/TTS/LLM/tools are all still Hume (`gauge-sandbox\lib\humeMicBridge.ts:218` real live mic bridge; `humeClient.ts:2908` `wss://api.hume.ai/v0/evi/chat`; `:3269` audio out; `:3208` tool_call; 75 schemas). Grep for kokoro/moshi/piper/coqui/xtts/vosk/deepgram → **zero across both trees**. `CORA_HANDSFREE_SPEC.md:3` — *"Status: spec only. Nothing below is built."* **Gauge's 60 s clips do NOT go to Hume** — they go to OpenAI Whisper via the backend (`audioLoop.ts:49 chunkDuration:60000` → `gaugeContext.tsx:394` → `whisperClient.ts:47`); the Hume batch path was already removed (`humeClient.ts:21-22`). What Gauge actually needs Hume for is **prosody scores on `user_message`** (`humeClient.ts:3286,3346`) — which arrive **only on Cora's conversational socket**. ⚠️ Also: there are **TWO** driver apps, both named `gauge-sandbox` in package.json — `C:\Users\troyh\gauge-sandbox` (DairyForge, `com.dairyforge.gauge`) and `C:\Users\troyh\aimforge-mobile` (AimForge, `com.aimforge.driver`), different EAS projects. | **NOT-STARTED** | Swapping Cora off Hume as stated would **silently delete Gauge's fatigue scoring**. First step: split the inbound router at `humeClient.ts:3177-3369` so `setOnHumeScores` no longer depends on the Cora socket's lifetime. *(This tree was handed to someone else — read-only.)* |
| **24** | Thinking Buddy: extract KB + prompts + tools from ElevenLabs, then swap the voice pipe | Project = `C:\Users\troyh\n8n_Pod_Uploader_jarvis\jarvis\` (HEAD `4e462d0` "Rename agent Jarvis to ThinkinBuddy"). **Extraction largely already exists but is stale**: `C:\Users\troyh\.tb_agent_config.json`, 73,819 B, dated **2026-05-14** — full agent dump incl. verbatim system prompt `:100` and five inline tool schemas (`:161,222,274,326,378`). Produced by an ad-hoc `Invoke-RestMethod`, not a saved script; its tool IDs are already superseded (`scripts\update-agent-config.mjs:36`). **There is no knowledge base to extract** — `:137-139 "knowledge_base": []`, `:143-144 "rag": {"enabled": false}`; memory is Vercel Blob + Neon pgvector. **Voice swap NOT-STARTED** — kokoro/moshi/piper/coqui/xtts → zero hits. | **PARTIAL** | Re-run the GET at `update-agent-config.mjs:149` and write a fresh dump; **skip KB extraction entirely, it is empty**. |
| **25** | Caldmere: Clerk/Supabase allow-list gating the build download | **NOT-STARTED, and three things are absent.** (i) **Clerk is not in this project at all** — `@clerk/*`/`ClerkProvider`/`CLERK_SECRET_KEY` → zero hits; `caldmere-login\package.json:12-13` lists only `@supabase/ssr` + `@supabase/supabase-js`. **The stack is Supabase.** (ii) **There is no download route to gate** — `src\app\download\page.tsx:18` `const DOWNLOAD_URL = "#download-coming-soon"; // [GAP: itch.io URL not found]`, rendered as a disabled `<span>` at `:94-115`; zero hits for `signedUrl\|createSignedUrl\|presigned`. (iii) **No allow-list of any kind** across app, launcher, and all 3 migrations. `src\middleware.ts:15-16 PROTECTED = ["/account","/characters"]` — **`/download` is deliberately public.** "Fatboy" → zero hits anywhere. | **NOT-STARTED** | The route must exist before it can be gated: create `src\app\api\download\route.ts` (session check → allow-list lookup → Supabase Storage signed URL), then add `/download` to `PROTECTED` at `middleware.ts:15`. |
| **26** | Discord webhook → shared-drive link for screenshot drops | **NOT-STARTED. Nothing on this machine posts *to* Discord.** One inbound-only gateway bot exists, duplicated byte-identically: `E:\UnityProjects\Caldmere\discord_listener.js:33-40` consumes `messageCreate` → appends to `feedback_log.txt`. No `discord.com/api/webhooks`, no `channel.send()`. `feedback_log.txt` does not exist, so it likely never ran. Shared-drive half: zero code. ⚠️ **A handoff claim is FALSE** — `paste-cache\3c1fd5779e46f6e7.txt:40-47` says *"Discord webhooks already set up, piping to Troy's E drive"*; **no webhook URL exists anywhere on this machine.** | **NOT-STARTED** | Troy creates a channel webhook in Discord server settings (his click — it mints the URL); store as `DISCORD_WEBHOOK_URL`; the reusable POST skeleton is `C:\Users\troyh\stripe_webhook_test.js:23-40`. |
| **27** | Browser-model grounding — same MEMORY/lessons context for Gemini web / Claude web | **NOT-STARTED. Zero artifacts.** Grep for `context.?pack\|paste.?bundle\|clipboard\|clip\.exe` across `~/.claude/scripts` and `E:\Helmion` → zero (positive control: `neon` in the same tree returns 5+ files, so the search was live). **The Helmion context MCP cannot serve this, on two independent grounds**: wrong content — `helmion_get_context` (`src/mcp/server.mjs:6`, dispatched `:110`) → `neon.mjs:384-407` returns only `helmion.blockers`/`autonomy_rules`/`context_entries`, **no MEMORY.md, no CLAUDE.md, none of the 20-file lessons library**; and unreachable — `src/mcp/server.mjs:107` binds `process.stdin`, and grep for `createServer\|express\|listen(\|SSE\|StreamableHTTP` across `src/mcp/` + `bin/` → **zero**. Stdio only; a browser tab has no path to it. | **NOT-STARTED** | One new script, no MCP and no network: concatenate `CLAUDE.md` + `memory/MEMORY.md` + the matching `lessons/lessons-*.md` into one paste-sized block, copy via `clip.exe`. |
| **28a** | SiteVector `stops.py` still unwired | **CONFIRMED still unwired.** `app\stops.py:85 stops_from_samples` is real, complete code (170 lines). **Every caller:** `tests\test_stops.py:18`, `tests\test_validation.py:32`, `tests\validate_real_gps.py:55`. **Zero production callers** — nothing in `app\` imports it. Both live ingest routes (`app\main.py:115` `POST /api/stops`, `:145` `/api/stops/bulk`) accept already-formed `StopIn` records (`:53-63`). The repo documents this honestly itself at `PHASE1.md:162`. | **CONFIRMED UNWIRED** | Read-only per instruction. |
| **28b** | SiteVector ms-vs-seconds duration blind spot | **CONFIRMED still live — and deliberately pinned open.** No unit conversion exists; the only guard is a range check. The arithmetic: `validate.py:67 MAX_DURATION_S = 86_400.0`; `:163 elif dur > MAX_DURATION_S -> FATAL`. A 45-second weigh emitted in **ms** arrives as `45_000.0`; `45_000 > 86_400` is **False**, so it ingests clean and reads as 12.5 hours. Then `engine.py:299 if max_dwell >= t.long_dwell_s` (`config.py:50 long_dwell_s = 300.0`) → True → `engine.py:308-310 score += 30` — **half the 60-point `promote_score` (`config.py:46`) from a single unit error.** Documented at `validate.py:58-66` and pinned by `tests\test_validation.py:132-150`, whose failure message reads *"this now rejects — the gap was closed, update this pin"*. **Contrast:** the same bug class **is** closed for `started_at` (`validate.py:141-153` detects epoch-ms → FATAL). ⚠️ **Cross-link:** `stops.py:80-81` (`v / 1000.0 if v > 1e11`) is the only code on disk that handles ms safely — **the safe path is the unwired one; the live path is the unsafe one.** | **CONFIRMED LIVE** | Feed-level, not record-level: in `_ingest_many` (`app\main.py:80`) check the `duration_s` distribution across the batch before the per-row loop, then flip the pin at `test_validation.py:147`. Git state: branch `master`, `fe0df00` 2026-07-26, clean except untracked `planning/`. |
| **22b** | Orphaned / unread files | **`BASE_RULES.md`** — cited as canonical by `CLAUDE.md:32` and `HELMION_CLAUDE.md:52`, but **no hook, script, or settings entry reads it**; its two rules survive only because `CLAUDE.md:19-36` restates them inline. **`HELMION_CLAUDE.md`** — exists; its only readers are Helmion's own writer and smoke test (`ClaudeProfileInstaller.cs:114`, `ProfileSyncEngine.cs:198`, `SmokeTests/Program.cs:383`). A product template masquerading as a live rule file. **Top-level `LEARNINGS.md`** — never read; `session_start_inject.sh:6-7` reads `planning/LEARNINGS.md`, a **different file**. **`BIG_SISTER_UNIFIED_SETUP_FINAL.md`** — **confirmed absent** from `~/.claude`, `D:\_Docs`, `E:\Helmion`, and OneDrive, yet still cited by `CLAUDE.md:19` and the Rule 0.27 block. | **CONFIRMED ORPHANED** | Either wire `BASE_RULES.md` into `session_start_inject.sh` or drop the "canonical" claim; drop or restore the `BIG_SISTER_UNIFIED_SETUP_FINAL.md` citation. |

---

## CLAIMS BY OTHER AGENTS THAT THIS AUDIT REFUTES

| Claim | Made in | Verdict |
|---|---|---|
| "Every OpenAI agent turn with tools 400s — `reasoning_effort` appears nowhere in Helmion source" | agent-K board row | **REFUTED.** `providers.mjs:134-136` sets `body.reasoning_effort = 'none'` for `providerId === 'openai'`, guarded to fire only when `toolDefs.length > 0` (`:122`). Present in HEAD (`fda4883`). Regression-tested by `test/openai-tool-turn.test.mjs` — 4 tests asserting the body on the wire, all pass. **OpenAI + tools is not broken.** |
| "Discord webhooks already set up, piping to Troy's E drive" | `paste-cache\3c1fd5779e46f6e7.txt:40-47` | **REFUTED.** No webhook URL exists anywhere on this machine; the only Discord artifact is an inbound-only listener whose output file was never created. |
| "`VOICE_PHASE1_WIRING_TODO.md:88` — send-to-model path is `MainWindow.xaml.cs:1418`" | that doc | **REFUTED.** Line 1418 is `ConsoleInputBox_OnPaste`. The real voice consumer is `:1714`. Stale citation. |
| "Gauge's 60-second scoring clips go to Hume" | Troy's handoff addendum premise | **REFUTED.** They go to OpenAI Whisper (`audioLoop.ts:49` → `gaugeContext.tsx:394` → `whisperClient.ts:47`); the Hume batch path was removed at `humeClient.ts:21-22`. Hume supplies **prosody scores**, which arrive only on Cora's socket. |

---

## TOP 5 THINGS TO DO NEXT — ranked by user-visible value

**1. Restore `BASE_RULES.md` and `LEARNINGS.md`, then disarm the thing that ate them.**
This is the only item on the list where something is *already gone* and every hour makes
recovery less likely. The two files on disk are Helmion product templates, proven
byte-identical to `ClaudeProfileInstaller.cs:185` and `:218`. No copy survives on any
path I can reach.
**First step:** Troy checks a backup only he can reach (another machine, a phone note, a
prior paste) for the 5,512 B / 105-line `BASE_RULES.md`. In parallel, delete lines
200-201 (`"LEARNINGS.md"`, `"LESSONS.md"`) — and arguably `"BASE_RULES.md"` at `:199` —
from the `approvedClaudeFiles` set in `ProfileSyncEngine.cs`, so no future caller can
target them regardless of the `overwriteExisting` flag.

**2. Un-hijack the slash key in the desktop app.**
Right now typing `/deploy staging` in the Pilot console **runs `deploy staging` as a
PowerShell command**. The full slash-command engine Troy asked for is built, tested, and
sitting one line away — the bridge already expands `/` inside `turn` at
`bridge.mjs:308-314`.
**First step:** at `MainWindow.xaml.cs:1488`, change the shell escape from `/` to a
different prefix (`!`), keeping the `/reset` special case, so slash text falls through to
`TurnAsync`.

**3. Make the desktop show which model it picked.**
The per-task router works and is proven to switch models on the wire, but the desktop
throws the answer away three times over — `AgentBridgeEvent` has no `Model` property, and
`MainWindow` has no `case "model":`. Troy asked for the UI to surface it; the CLI does
(`session.mjs:427-428`) and the app he actually uses does not.
**First step:** add `string? Model`, `string? Tier`, `string? Reason` to
`AgentBridgeEvent` (`AgentBridge.cs:498-515`) and read them in `ParseEvent` (`:313-349`);
the label comes next.

**4. Fix the mic that reports "Listening" while dead, then wire dictate mode.**
Two voice defects with direct daily cost. The watchdog bug means unplugging or losing a
mic mid-session leaves the UI claiming it is listening while zero audio arrives — a
silent failure, the worst kind. And `DictationCommands.cs` implements all four of Troy's
spoken commands with **zero production callers**.
**First step:** call `SafeDisposeCapture()` from `WhisperSpeechRecognizer.cs:551` when
`e.Exception is not null`. Then branch to `DictationCommands.Detect(heard)` after the
`_voiceTurnBusy` guard at `MainWindow.xaml.cs:1725`.

**5. Rule on the CLI shell default, and close the redaction pattern gaps.**
Both are one-decision security items. `helmion agent` runs arbitrary shell with no
prompt (`session.mjs:89`) while the desktop starts read-only — that asymmetry is
undocumented. Separately, `redact.mjs:9` misses `postgres://` on a typo (`postgresql?:`
requires the `l` to be optional, not the whole word) and there is no generic
`KEY=value` rule, so a `.env` read leaks every credential without a known vendor prefix.
**First step:** Troy rules on the CLI default (match desktop's read-only, or keep full
and say so in `--help`). Independently, fix the `redact.mjs:9` regex to
`postgres(ql)?:\/\/` and add a generic `KEY=<value>` rule — that one is safe to do now.

---

## SCOREBOARD

| Verdict | Count | Items |
|---|---|---|
| ✅ DONE-VERIFIED | 12 | 2, 4, 6, 9, 10, 11, 16a, 16d, 17, 19, 20, 28-probes |
| 🟠 PARTIAL / DONE-BUT-UNWIRED | 9 | 1, 2b, 3, 5, 7, 12, 16b, 16c, 18 |
| 🔵 NOT-STARTED (correctly) | 8 | 13, 15, 23, 24, 25, 26, 27, agent-os-real-install |
| 🔴 BLOCKED | 2 | 14 (VRAM), 21 (Troy's DDL approval) |
| 🔴 STILL-OPEN BUGS | 4 | a (both directions), b (unruled), c, d (already fired) |
| ⚠️ REFUTED CLAIMS | 4 | see table above |

**The honest headline:** the engineering shipped today is real and unusually
well-tested — 298 Node tests, 10 smoke suites, positive controls on most of it. The
systematic failure is not quality, it is **reachability**. Five separate features
(slash commands, dictate mode, the model switcher's UI half, the MCP install pipeline,
the voice backend selector) are complete, green, and unreachable from the application
Troy actually opens. A board that reads "done" on all five is not lying about the code;
it is measuring the wrong end of the chain.
