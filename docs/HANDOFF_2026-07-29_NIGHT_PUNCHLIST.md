````
════════════════════════════════════════════════════════════════════════
RULE 0.001 — SOURCE OF TRUTH + FULL TRACING (quoted verbatim, read first)

  GATE 1 — SOURCE OF TRUTH. Cite the primary source where the claim lives.
  Code → file:line. API → response body + endpoint hit. Vendor docs → URL +
  quoted sentence. DB → query + row. NOT "memory says," NOT "the handoff
  says," NOT "I recall." Memory and prior handoffs are SECONDARY — every
  claim starts [UNVERIFIED] until the primary source confirms it THIS session.

  GATE 2 — FULL TRACING. For any multi-stage claim ("X causes Y," "feature is
  wired," "demo works"), draw the chain explicitly with a citation on EVERY
  link. A chain with one uncited link IS the lie.

RULE 0.002 — CITE OR SHUT UP. If I cannot cite a primary source for a claim
right now, I do not make the claim.

RULE 0.13 — RETIRED. There is NO 45/60-minute handoff cap.

EVERY CLAIM BELOW IS [UNVERIFIED] FOR YOU UNTIL YOU RE-CHECK IT THIS SESSION.
════════════════════════════════════════════════════════════════════════
````

# HANDOFF 2026-07-29 night — working a punch list, 3 of 8 done

## Why this file exists

The previous session (`adcdf4dc`) was stopped by Troy at **20:03:46** and its main
transcript is **0 bytes**. Everything it was doing had to be reconstructed from
subagent `.meta.json` files. Troy is restarting with transcript saving ON so that
cannot happen again. Do not repeat the recovery — it is all written down here.

## The punch list, and where it stands

| # | Item | State |
|---|---|---|
| 1 | Push the tested panel work | **DONE** — `2935ab2`, pushed |
| 3 | Unsupported-confidence detector | **DONE** — `9b5245e`, pushed, 29 tests |
| — | Push session failures + successes to Neon (Rule 0.28) | **DONE** — 9 rows in `bigsister.agent_logs` |
| 2 | Autonomy hook fail-open fix | **TESTED 6/6, NOT APPLIED — needs Troy** |
| 8 | Wire the detector into the extension | not started |
| 4 | Verify the unreviewed extension guard changes | partly read, looks sound |
| 5 | Test `GuardAuditLog.cs` (374 lines, zero coverage) | not started |
| 6 | `advisory-lane.mjs:101,132` names a SQL file that does not exist | not started |
| 7 | Redo the lost two-sandbox plan | not started |

`git rev-parse HEAD` = `9b5245e`, **0 unpushed**. Working tree carries only
`desktop/Helmion.Desktop/Assets/Helmion.ico` (regenerated by
`Helmion.Desktop.csproj:22-25` on EVERY Windows build — the diff is generator
noise, leave it out of commits) plus Troy's own `guard_lab/`, `guard_test.txt`
and `_review_export_primary_providers_2026-07-28/`. **`guard_lab/` is Troy
testing the guard live — do not touch it.**

## ITEM 2 IS THE ONE BLOCKING THING. Read this before anything else.

`C:\Users\troyh\.claude\scripts\hook_autonomy_boundary.ps1:35` was:

```powershell
try { $payload = $stdin | ConvertFrom-Json } catch { exit 0 }
```

`exit 0` is **ALLOW**. Any payload the gate could not parse skipped all 342 lines
of Tier A / Tier B logic below it. **Measured against the LIVE hook**, not
theorised: an `ALTER TABLE` command behind a UTF-8 BOM returned **exit 0**, and a
payload of pure garbage returned **exit 0**.

The fixed file is at **`C:\Users\troyh\hook_autonomy_boundary.FIXED.ps1`** and its
6-case validator is at **`C:\Users\troyh\hook_autonomy_boundary.VALIDATE.ps1`**.
They were saved out of the session scratchpad on purpose so a restart cannot lose
them.

**Diff footprint, verified:** 2 lines removed, 48 added, and *nothing else in the
file changes* — all Tier A/B gate logic below is byte-identical.

**Validation, all 6 green on the candidate:** benign command → 0; BOM + benign →
0 (BOM handled, not denied); schema change → 2; **BOM + schema change → 2 (the
hole)**; garbage payload → 2; empty stdin → 0 with a stderr note. The same cases
against the LIVE hook fail rows 4 and 5, which is the defect.

**Two traps inside this fix, both measured, do not re-derive them:**

1. `String.StartsWith(string)` is **culture-sensitive** and U+FEFF is an
   *ignorable* character under culture rules, so `$s.StartsWith([string][char]0xFEFF)`
   returns **true for every string** — a first attempt at the fix ate the leading
   `{` of valid JSON and denied a harmless `git status`. Use ordinal comparison.
2. `[Console]::In.ReadToEnd()` decodes with `Console.InputEncoding`, which measured
   as **ibm437** in a spawned pwsh here — the BOM bytes `EF BB BF` arrive as
   `U+2229 U+2557 U+2510`, **not U+FEFF**. So a BOM cannot be stripped at the
   string level at all. The fix reads stdin as **bytes** via `StreamReader` with
   `detectEncodingFromByteOrderMarks`, which consumes the BOM before decoding.

**Why it is not applied:** the auto-mode classifier denied it three times —
Copy-Item, then Edit, then Copy-Item again — because an agent rewriting its own
security gate should require a human. That is correct behaviour. Troy authorized
it explicitly in writing; the harness still blocks it. **He must either add a
permission rule or run the command himself.**

**Why it cannot be "committed" as he asked:** `~/.claude` is **not a git
repository** (`git rev-parse` → *fatal: not a git repository*). And it must NOT be
committed into Helmion either — the hook body names DairyForge, AimForge and the
`bigsister` trust lanes, and Helmion is the repo he is selling under BUSL. See
`memory/project-2026-07-29-helmion-share-with-bryce.md`.

**One open judgment call, reviewed by Grok via the advisory lane:** on *genuinely
empty* stdin the fix allows (exit 0) with a stderr warning rather than denying.
Grok's position, independently: correct, because denying would convert a harness
fault into a total lockout of every concurrent session including any attempt to
repair the hook — and it flips only if an out-of-band recovery path exists that
does not itself need a gated tool call. None does.

## The panel: Troy has NOT seen it yet, and the reason is not a bug

He opened the Pilot at 21:01 and reported "nothing's changed on the side panel."
Cause, cited: he launched
`E:\Helmion\artifacts\Helmion-Pilot-win-x64-self-contained\Helmion Pilot.exe`,
**built 17:45:05**. The panel source was written at **19:57:31**
(`MainWindow.xaml`) and **20:01:12** (`MainWindow.GuardPanel.cs`) — the binary
predates the panel by 2h16m. **The artifact must be republished
(`npm run desktop:package`) and it cannot be while the Pilot holds the exe open.**

Where the panel renders, so you can tell him what to look for: a **336 px column
down the right side** — `MainWindow.xaml:981` `GuardPanelColumn`, chrome at
`MainWindow.xaml:3282` `GuardPanelChrome`, headline chip → tab strip → cards.

## What landed, with the evidence

`2935ab2` — 237 smoke checks for the panel (93 escalation, 76 feed, 68 lease
inspector). `GuardEscalation.cs:72` had documented a `GuardEscalationChecks` class
in the smoke suite that **was never written**, and the build stayed green. Two real
defects fixed: `GuardFeed.WorstLevel` ranked `Unknown` (enum 0) as *better* than
`Normal` (enum 1), so the headline read **OK** above a grey UNKNOWN card —
contradicting that file's own doc at lines 326-330; and `Program.cs:153` still
asserted `Lease.Status == "UNAVAILABLE"`, pinning a removed lie in place, which
means `npm run desktop:test` was **already RED** on the uncommitted tree.

`9b5245e` — `src/core/unverified-claims.mjs`, batch item 5. Two signals, never one
alone: a confidence marker AND a checkable referent in the **same sentence**. It
flags and never blocks (`blocked` is a hardcoded false the suite pins). Stage one
is 5.7 ms over a 100 KB reply, no model call. Two defects caught by cases written
to fail first: the judgment suppression list matched by **substring**, so "prefer"
matched inside "Preferences" and silently deleted a true positive; and `/api/loads`
was reported as a **file path** because file-path was ordered before endpoint.

Suites now: `npm test` **643 pass / 0 fail**, `npm run check` exit 0,
`npm run desktop:test` 468 checks, `npm run desktop:build` 0 errors.

## Machine state worth knowing

C: went **16.2 → 23.8 GB free**. The 2026-07-18 cleanup checklist is **exhausted**
— see `memory/c-drive-cleanup-2026-07-18.md`, rewritten today. Seven package
caches now redirected to `E:\caches` by user env vars, verified by registry
read-back. **Still available: 8.9 GB of node_modules across 11 project copies idle
7-19 days — the auto-mode classifier denied that sweep, it needs Troy.** The uv
cache (5.31 GB) is held by live `uvx` MCP processes and frees on restart.

## How to talk to Troy

Short, plain sentences, one paragraph when he asks for one. Tables for lists.
**Work from the punch list and report once** — he called out, correctly, that a
session with no punch list and no plan wastes his night. Never mention the hour or
tell him to sleep. **Never start voice** — text only until he types the literal
word "voice". His dictation garbles words; use context, never quiz him on one odd
word ("hellman" = Helmion).
