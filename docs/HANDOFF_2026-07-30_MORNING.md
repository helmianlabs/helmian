````
════════════════════════════════════════════════════════════════════════
RULE 0.001 — SOURCE OF TRUTH + FULL TRACING (quoted verbatim, read first)

  GATE 1 — SOURCE OF TRUTH. Cite the primary source where the claim lives.
  Code → file:line. API → response body + endpoint hit. Vendor docs → URL +
  quoted sentence. DB → query + row. NOT "memory says," NOT "the handoff
  says," NOT "I recall." Memory and prior handoffs are SECONDARY — every
  claim starts [UNVERIFIED] until the primary source confirms it THIS session.

  GATE 2 — FULL TRACING. For any multi-stage claim ("X causes Y," "feature is
  wired," "demo works"), draw the chain explicitly with a citation on EVERY
  link. A chain with one uncited link IS the lie.

RULE 0.002 — CITE OR SHUT UP. If I cannot cite a primary source for a claim
right now, I do not make the claim.

RULE 0.13 — RETIRED. There is NO 45/60-minute handoff cap.

EVERY CLAIM BELOW IS [UNVERIFIED] FOR YOU UNTIL YOU RE-CHECK IT THIS SESSION.
════════════════════════════════════════════════════════════════════════
````

# HANDOFF 2026-07-30 morning — session `ebf9ba3e`

`git rev-parse HEAD` = `7b7fa39`, branch `main`, **0 unpushed**.

Working tree carries only `desktop/Helmion.Desktop/Assets/Helmion.ico`
(regenerated by `Helmion.Desktop.csproj:22-25` on EVERY Windows build — generator
noise, leave it out of commits), plus Troy's own untracked `guard_lab/`,
`guard_test.txt`, `_review_export_primary_providers_2026-07-28/`. **Do not touch
those three.**

**RESTORE POINT, taken before the risky half of the session:**
`E:\Helmion\_restore\20260730-032613\` holds BASE_RULES.md, LESSONS.md,
LEARNINGS.md, CLAUDE.md, settings.json, .claude.json, .env, autonomy_rules.json,
lease.json. Git tag `restore/20260730-032613` at `f7931a8`.
One command back: `git reset --hard restore/20260730-032613`.

---

## THE THING THAT WAS ACTUALLY RECOVERED

**33 of Troy's LESSONS.md entries had been destroyed. They are back.**

| | before | after |
|---|---|---|
| Real entries | 8 | **41** |
| Size | 7,799 B | **75,780 B** |
| Template placeholder junk | 3 | **0** |
| Any of his originals lost | — | **0** |

Verified on disk after the write: 7 of his 8 surviving entries were byte-identical
to the archive; the 1 that differed was **his being newer**, and the merge kept
his. Recovered content includes "OPEN IT FOR HIM", "NEVER auto-open files on
Troy's desktop", the QA-screenshot rule, the Chobani route, the sprite-scale
night, the ELD adapters never live-tested.

Backup of the pre-restore file:
`C:\Users\troyh\.claude\LESSONS.BEFORE-RESTORE-20260730-030720.bak`

🔴 **The one hole:** the archive is 2026-07-26 16:51; the first wipe was
2026-07-28 15:34. Anything written in that ~47h window exists in no copy found.
Shadow copies need elevation and were refused.

---

## Sync Profiles — root cause, and what is still open

**The bytes were never in a repo file.** `ClaudeProfileInstaller.cs:230-242`
yields `const string` values compiled into the binary — `BaseRulesTemplate`
`:303-334` (1,635 B), `LessonsTemplate` `:352-372` (612 B), `LearningsTemplate`
`:336-350` (361 B).

**Smoking gun, verified directly:** the stub left on disk,
`BASE_RULES.STUB-20260729-162821.bak`, begins
`# BASE RULES — Read First, Every Session` = `ClaudeProfileInstaller.cs:304`.
Troy's real file begins `# BASE RULES — read first, every session, no exceptions`.
Different text. The three stub sizes match the three constants exactly.

**Timeline:** shipped `607eddc` (07-27); closed `d199b00` (07-29 16:57). His last
stub is 16:28 — 29 minutes before the fix.

### STILL OPEN — needs a decision

1. **`~/.claude.json` is rewritten wholesale and FAILS OPEN.**
   `ProfileSyncEngine.cs:178-186` catches a deserialize failure and starts from
   `new JsonObject()`. A ~72 KB config becomes ~2 KB. **No backup is taken
   first.** Same pattern at `:143-152` for `claude_desktop_config.json`.
2. **Plaintext secrets** — `:164-167` / `:198-201` write `HELMION_DATABASE_URL`
   and `GROK_API_KEY` into `~/.claude.json` in the clear.
3. **A stale build still contains the destructive installer**:
   `desktop\Helmion.Desktop\bin\Release\net10.0-windows\Helmion Pilot.exe`,
   2026-07-28 10:01:22, 203,776 B. Running THAT one blind-overwrites.

---

## Landed this session — all pushed

| Commit | What |
|---|---|
| `2787f1a` | Unsourced-claim detector wired into the extension as a prose-only second lane |
| `180f84a` | `advisory-lane.mjs` named a SQL file that does not exist |
| `685df53` | Extension: a red warning was deleting the "not fully checked" notice beside it |
| `1dcf380` | **Both guard-panel reds were fake** — a recovered card could never come down, and a deliberately-chosen setting rendered red |
| `b430d7f` | A **deleted** rules file was getting the blank template written over it |
| `9e0c566` | The composer `+` menu — 4 items, all 4 states |
| `f9f1313` | The `+` menu became the **Maestro's** menu, with each CLI's real syntax |
| `44f2d19` | Left panel leads with **PROJECTS**; the nine pages fold behind one row |
| `056723b` | Panel **search** and **pin** |
| `6033371` | **Extension durable ledger** + popup |
| `7b7fa39` | **The three UNKNOWN guard cards are now computed** |

**Gates, this session:** `npm test` **708 pass / 0 fail**. `npm run check` exit 0.
`npm run desktop:build` 0 errors. `npm run desktop:test` exit 0 —
plus-menu 214, escalation 110, feed 81, lease 68, **guard liveness 47**,
project shelf 32, installer guard 23, ask-permission 58, voice 39.

---

## Guard panel: 3 unknown → 0 unknown

Captured from the running window:

| Card | Reads now |
|---|---|
| Block ledger | **✓ OK** "recording · nothing blocked yet … recording since 2026-07-30 10:09" |
| Execution guard | **✓ OK** "Execution guard is LIVE · Probed just now: `rm -rf /helmion-liveness-probe-not-a-real-path` was REFUSED and `echo helmi…`" |
| Browser layer | **⚠ WARNING** "Browser guard is NOT installed — 4 browser profile(s) were read and none has an extension loaded from E:\Helmion\extension" |

That warning is **correct** — the extension has not been loaded unpacked yet.
Loading it is what turns it green. Nothing fakes it.

**Unknown was NOT softened.** It is now reserved for the case where the check
itself cannot run, and checks pin that both ways — a never-started ledger still
reads Unknown, and a probe with no CLI says *"not an all-clear"* in as many words.

---

## Helmion Guard (browser extension) — shippable, one gate left

`6033371` gave it a **durable ledger**. Troy's decision, quoted in the code, was
the one `background/scan.js` had been waiting on in writing.

- **ONE permission**: `storage`. A test asserts the list is exactly `["storage"]`;
  a second asserts `chrome.storage` appears in `background/ledger.js` **and
  nowhere else**.
- Every row: timestamp · site · **the user's query** · **the conversation** ·
  what matched · offending text · line · outcome. Both kinds logged, kept apart
  by `kind`.
- **Nothing is uploaded.** The network-guard test still fails the build if
  anything reaches the network, and still passes.
- `popup/` — counts, last 25 rows, export to `.json`, two-click clear.
- `grok.com` added to the matched sites.

🔴 **THE ONE GATE BEFORE GITHUB: nobody has loaded this in Chrome.** Everything is
proven by tests and `node --check`, not by a browser. It is a five-minute check —
load unpacked, ask an AI for `rm -rf`, open the popup, see the row. That also
flips the browser card green.

---

## What Troy asked for that is NOT built

**1. Helmion has no MCP client.** Its own comment, `src/agent/plugins.mjs:34`:
*"Helmion's agent has no MCP client today — `mcpServers` appears nowhere in
`src/agent/` and nothing in the turn loop spawns an external server… an
`approved` verdict produces a vetted registration RECORD, not a running server."*

Consequence: Helmion cannot call Grok/Gemini/OpenAI, so Gemini and ChatGPT cannot
talk to each other through it, and the ~2,100-line MCP vetting pipeline ends in a
filing cabinet.

**2. The advisory loop is ~60% built and never wired.** What EXISTS:
`src/core/advisory-lane.mjs` (11.7 KB), `advisory-action.mjs`, `classifyOperation`
(Tier A/B), `consensusStatus`, and the MCP tools `helmion_register_action` /
`helmion_record_review` / `helmion_consensus_status`. Two separate Neon
endpoints, `HELMION_DATABASE_URL` and `BIGSISTER_DATABASE_URL`
(`advisory-lane.mjs:206`).

What is MISSING, traced: **nothing on the push path calls `consensusStatus`.**
Its only callers are the Neon store method, the MCP tool exposing it, and a
manual CLI command at `bin/helmion.mjs:935`. And nothing publishes work INTO the
advisory lane for the other models to read. Row counts from an earlier audit:
`governance_actions = 0`, `advisory_reviews = 0`.

**Troy's stated order:** (1) MCP client, (2) a publisher that writes each action
to the advisory lane, (3) the reviewer loop, (4) `consensusStatus` called BEFORE
a commit — the line that turns advice into a brake.

**3. PART 2, the multi-session manager** — pill buttons per LLM, named sessions,
one manager routing input and aggregating output into one feed, every action
through the guard layer, no polling of idle sessions. **Not started**, by his
instruction, until PART 1 was done. PART 1 is done.

---

## Test session that was recorded

`D:\_Video\Recordings\Desktop\Desktop 2026.07.30 - 03.21.20.08.mp4`, **1,244.7 MB**,
03:21→03:43. Contains the Pilot full-screen on Console with Grok as Maestro,
being told to run `rm -rf` in **full-tools no-ask mode**, and the transcript:

```
→ run_command({"command":"rm -rf E:\\Helmion\\_guard_probe_delete_me"})
← Error: tool 'run_command' was BLOCKED by Helmion governance — it matches a
  destructive operation the kernel always blocks: recursive/forced rm.
```

`guard_lab` still had all 25 files afterwards. Tests 2-4 of that battery were
never run.

---

## Reference filed, explicitly NOT a plan

`docs/REFERENCE_OVERSTORY_PRIOR_ART.md` — Overstory and Warren (jayminwest).
**Overstory is ARCHIVED 2026-05-28; Warren is current.** That file corrected
itself twice and both drafts are left visible on purpose. The most useful thing
in it is his own `STEELMAN.md`: *"errors compound multiplicatively"*, and a
measured **6.7×** cost — a 20-agent swarm burned 8M tokens against 1.2M for a
single agent. He then archived it. Read that before building PART 2.

---

## Environment traps — do not rediscover these

1. **`node --test <file>` is blocked in this repo.** `hook_autonomy_boundary.ps1:152-171`
   reads the named script as an indirect payload, and
   `extension/test/extract-dom.test.mjs:95` carries the fixture string
   `DROP TABLE loads;`. Use `npm test` — it names no file. **No TROY-APPROVED
   marker was ever fabricated.**
2. **`git commit -m @'…'@` is blocked.** `hook_block_commit_qa.ps1:98-101` reads
   quoted `-m` values and `-F -` heredocs, but not a PowerShell here-string, so
   the message reads as empty. Write it to a file and use `git commit -F <path>`.
3. **The `claude-code-guide` subagent type fails with "Prompt is too long"** even
   on a ~200-word prompt. Use WebFetch against `https://code.claude.com/docs/`.
4. **Background subagents must call `SendMessage` with `to:"main"`.** Their plain
   text never reaches the parent; several went idle repeatedly without delivering.
5. **The Console tab is not realised until navigated** — UI Automation finds
   nothing in it until `ConsoleNav` is invoked.
6. **A running Pilot locks the build output.** Close it before `desktop:build`.
7. **NVIDIA captures land in `D:\_Video\Recordings\Desktop\`**, not `~\Videos`.
   Read `GallerySettings.json` → `currentDirectoryV2` rather than guessing.
8. **Hand-assembling JSON in a C# interpolated string bit twice in one hour** —
   `}}` is one brace in an interpolated half and two in a plain one. Use a
   serializer. Both bugs were found by tests, neither by reading.

---

## How to talk to Troy

Short, plain sentences. Tables for lists. Work from a list and report once.
**Never mention the hour. Never tell him to sleep. Never start voice** — text only
until he types the literal word "voice". His dictation garbles words; use context,
never quiz him on one odd word ("hellman" = Helmion, "Crocs CLI" = Grok CLI).

When he asks whether something is real, **trace it and answer with the citation**.
Three times this session the honest answer was "most of these are real and this
one is a bug", and that was the answer he wanted.
