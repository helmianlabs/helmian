````
════════════════════════════════════════════════════════════════════════
RULE 0.001 — SOURCE OF TRUTH + FULL TRACING (quoted verbatim, read first)

  GATE 1 — SOURCE OF TRUTH. Cite the primary source where the claim lives.
  Code → file:line. API → response body + endpoint hit. Vendor docs → URL +
  quoted sentence. DB → query + row. NOT "memory says," NOT "the handoff
  says," NOT "I recall." Memory and prior handoffs are SECONDARY — every
  claim starts [UNVERIFIED] until the primary source confirms it THIS session.

  GATE 2 — FULL TRACING. For any multi-stage claim ("X causes Y," "feature is
  wired," "demo works"), draw the chain explicitly with a citation on EVERY
  link. A chain with one uncited link IS the lie.

RULE 0.002 — CITE OR SHUT UP. If I cannot cite a primary source for a claim
right now, I do not make the claim.

RULE 0.13 — RETIRED. There is NO 45/60-minute handoff cap.

EVERY CLAIM BELOW IS [UNVERIFIED] FOR YOU UNTIL YOU RE-CHECK IT THIS SESSION.
════════════════════════════════════════════════════════════════════════
````

# HANDOFF 2026-07-30 → Coach

Session `ebf9ba3e`. Repo `E:\Helmion`, branch `main`, **0 unpushed**.
`git rev-parse HEAD` = `9e0c566`.

Working tree carries only: `desktop/Helmion.Desktop/Assets/Helmion.ico`
(regenerated by `Helmion.Desktop.csproj:22-25` on EVERY Windows build — generator
noise, leave it out of commits), plus Troy's own untracked `guard_lab/`,
`guard_test.txt`, `_review_export_primary_providers_2026-07-28/`. **Do not touch
those three.**

---

## THE ONE THING WAITING ON TROY

**33 of his LESSONS.md entries were destroyed and they are recoverable. A merged
file is built and verified, sitting in scratch, unwritten.**

| | Entries | Size |
|---|---|---|
| Live `C:\Users\troyh\.claude\LESSONS.md` | **8 real** + 3 template placeholders | 7,799 B |
| Recovered bundle | **41 real** | 75,696 B |
| Merged candidate (staged, NOT applied) | **41**, 0 placeholders | 75,780 B |

Bundle source, verified present this session:
`C:\Users\troyh\Documents\Codex\2026-07-26\realtime-voice-chat\outputs\HELMION_SOURCE_BUNDLE\LESSONS.md`

Merged candidate:
`…\Temp\claude\E--Helmion\ebf9ba3e-…\scratchpad\LESSONS.merged.md`

**The merge is strictly additive.** 7 of his 8 surviving entries are byte-identical
to the bundle's. The 1 that differs — `## 2026-07-26 — distill self` — is **his
newer version** (it adds *"Severity starts at 'flag'; only a human promotes it to
'block'"*), and the merge keeps HIS and discards the bundle's older text. Nothing
of his is lost.

Among the 33 destroyed: *"OPEN IT FOR HIM. Don't explain, don't offer, don't
diagnose"*, *"NEVER auto-open files on Troy's desktop"*, the QA-screenshot rule,
the Chobani route, the sprite-scale night, the ELD adapters never live-tested.

**Gap I could not close:** the bundle is 2026-07-26 16:51; the first wipe is
2026-07-28 15:34. Anything written in that ~47h window exists in no copy found.
Shadow copies need elevation and were refused. Unknown whether anything is there.

**Do not apply this without Troy saying so.** Writing to a living document is the
exact class of action that caused this. Plan when he says go: timestamped `.bak`
of the live file first, write, then re-read size + first line to prove it landed.

---

## Sync Profiles — root cause, traced end to end

**The bytes were never in a repo file.** `ClaudeProfileInstaller.cs:230-242`
`TemplateFiles()` yields `const string` values compiled into the binary:
`BaseRulesTemplate` `:303-334` (1,635 B), `LessonsTemplate` `:352-372` (612 B),
`LearningsTemplate` `:336-350` (361 B). `ProfileSyncEngine.cs:37-74` does the same
for GEMINI.md.

**Smoking gun, verified by me not by an agent:** the stub left on disk,
`BASE_RULES.STUB-20260729-162821.bak`, first line is
`# BASE RULES — Read First, Every Session` = `ClaudeProfileInstaller.cs:304`.
Troy's real file's first line is
`# BASE RULES — read first, every session, no exceptions`. Different text. And
the three stub sizes match the three constants exactly: **1,635 / 612 / 361**.

**Timeline:** destructive write shipped `607eddc` (2026-07-27); closed `d199b00`
(2026-07-29 16:57:26). His last stub is timestamped **16:28** — 29 minutes before
the fix. That is why the press he made at ~23:05 on 2026-07-29 was clean.

**Measured live:** Troy pressed Sync Profiles at ~23:05 against a build made
22:55:57 from current source. All three files came back byte-identical with
**mtimes unchanged** and **no new .bak**. Skipped at
`ClaudeProfileInstaller.cs:146` via `isLiving`, hitting `continue` at `:149`
before the backup at `:152`.

### Fixed this session — `b430d7f`

The last live hole: every guard is nested inside `if (File.Exists(targetPath))`
at `:141`, so a **missing** living document got no protection and was seeded with
the blank template — indistinguishable from a successful restore. Now a backup
beside a missing living document is treated as proof it existed, and the
installer recovers from the newest backup **that is not itself a template**
(the broken button produced its own stub backups), and says `RECOVERED` out loud.
With nothing to recover from it still seeds — that really is a new machine.

### STILL OPEN — not fixed, needs a decision

1. **`~/.claude.json` is rewritten wholesale and fails OPEN on a parse error.**
   `ProfileSyncEngine.cs:178-186` catches a deserialize failure and starts from
   `new JsonObject()`. A ~72 KB config becomes ~2 KB containing only mcpServers +
   hooks. No backup is taken first. Same pattern at `:143-152` for
   `%APPDATA%\Claude\claude_desktop_config.json`.
2. **Plaintext secrets.** `ProfileSyncEngine.cs:164-167` / `:198-201` write
   `HELMION_DATABASE_URL` and `GROK_API_KEY` into `~/.claude.json` in the clear.
3. **A stale build still contains the destructive installer.**
   `E:\Helmion\desktop\Helmion.Desktop\bin\Release\net10.0-windows\Helmion Pilot.exe`,
   2026-07-28 10:01:22, 203,776 B. Running THAT one blind-overwrites. Not deleted.

---

## Landed this session — all pushed

| Commit | What |
|---|---|
| `2787f1a` | Unsourced-claim detector wired into the browser extension as a second, prose-only lane. Advisory: never masks, never touches the red badge. |
| `180f84a` | `advisory-lane.mjs` told the operator to apply `sql/bigsister/001_advisory_output_review_state.sql`. No such directory. Now points at `docs/FLYWHEEL_AUDIT_2026-07-28.md` §6.5, which holds the real DDL. |
| `685df53` | Extension: `clearWarning` ended by calling `clearUnchecked`, so a block that was BOTH dangerous and part-unchecked drew the amber "did not check line N" notice and destroyed it one line later. |
| `1dcf380` | **Both guard-panel reds were fake.** (a) `GuardFeed.Observe` only ever raised a level, so "Local service connected" stayed CRITICAL after recovery, count climbing ×6→×7. (b) Full permissions rendered red for a setting chosen from a dropdown — and its twin: `Ask`, the safest mode, rendered yellow. |
| `b430d7f` | Missing-living-document recovery, above. |
| `9e0c566` | The `+` menu. PART 1 of the build directive. |

**Gates, run this session:** `npm test` 692 pass / 0 fail. `npm run check` exit 0.
`npm run desktop:build` 0 warnings 0 errors. `npm run desktop:test` exit 0.
Suite counts: plus-menu **125** (new), escalation 110, feed 81, lease 68,
installer guard 23, ask-permission 58, voice 39.

---

## The build directive — where it stands

**PART 1 (the + menu) — DONE, `9e0c566`.** Four items with plain-language lines;
all four states (in-progress / success / failed-with-reason / remove-undo) on all
four kinds. Upload names too-large and unsupported-type with actionable
sentences. State machine is a pure object in `Helmion.Desktop.Core/PlusMenu.cs`
so the two states that never get tested can be tested without a window.

Runtime evidence: app launched, driven to Console via UI Automation, button found
in the live tree (`FOUND ConsolePlusButton name='+' enabled=True`) and invoked;
screenshot shows the popup open with all four rows and descriptions.

🔴 **NOT PROVEN AT RUNTIME:** no screenshot yet shows a state ROW rendered in the
live window. Proven by the 125 checks; the XAML binds the same properties; the
binding itself has not been watched working. UI Automation could not reach the
popup rows — they render inside the main HWND and carry no automation Name
because their content is a panel. One manual click settles it: `+` → Connectors
with the box empty must show a red Failed row.

🔴 Skills / Connectors / Plugins are wired to real backends
(`AgentBridge.ListCommandsAsync`, `McpSecurityRunner.DiscoverAsync`,
`.helmion/plugins.json`) but **none has been clicked at runtime**.

**PART 2 (multi-session manager) — NOT STARTED**, per his explicit instruction not
to begin until PART 1 is finished end to end. Full spec is in his message: pill
buttons per LLM with config-driven colours, one session per press, nameable via
terminal, coding agents get file/command access through the guard layer, API-only
models get Helmion as their hands **also through the guard layer**, one manager
session routing input and aggregating output into a single labelled feed, and
**no polling of idle sessions** because every call spends his credit.

---

## Environment gotchas that cost time — do not rediscover these

1. **`node --test <file>` is blocked in this repo.**
   `hook_autonomy_boundary.ps1:152-171` reads the named script as an indirect
   payload; `extension/test/extract-dom.test.mjs:95` has carried the fixture
   string `DROP TABLE loads;` since the extension was written; `:213` promotes it
   to a SQL literal and `:250` calls it a schema change. Use `npm test` — it
   names no file. **No TROY-APPROVED marker was fabricated.**
2. **`git commit -m @'…'@` is blocked.** `hook_block_commit_qa.ps1:98-101` reads
   quoted `-m` values and a `-F -` heredoc, but not a PowerShell here-string, so
   the message reads as empty and a documented commit is refused. Write the
   message to a file and use `git commit -F <path>`.
3. **The `claude-code-guide` subagent type fails with "Prompt is too long"** even
   on a ~200-word prompt. Failed twice. Use WebFetch against
   `https://code.claude.com/docs/en/commands` instead.
4. **Background subagents' plain text does not reach main.** They must call
   `SendMessage` with `to:"main"`. Both agents went idle repeatedly without
   delivering until told this explicitly.
5. **The Console tab is not realised until navigated.** UI Automation cannot find
   anything in it until `ConsoleNav` is invoked.

---

## Machine state

C: **22.5 GB free / 215.3 GB used** — *up* from the 16.2 GB recorded earlier
2026-07-29, so the squeeze predates this session. Measured, not guessed:

| Where | GB |
|---|---|
| `AppData` | 41.8 (Google 6.8, Programs 5.8, **uv cache 5.3**, Temp 3.0) |
| OneDrive | 12.2 |
| gauge-sandbox | 7.2 (another owner — do not touch) |
| ~12 near-duplicate project trees (aimforge ×4, forge ×3, df-wt-* ×3) | ~10 |

Every file over 200 MB written to C in the five hours before the scan was a
OneDrive database (3 files, 0.81 GB total). **Nothing this session put gigabytes
on C** — builds and the 101 MB artifact land on E:.

Reclaimable without touching anyone's work: uv cache 5.3 GB (frees on restart of
the `uvx` MCP processes), `AppData\Local\Temp` 3.0 GB.

---

## How to talk to Troy

Short, plain sentences. Tables for lists. Work from a list and report once — he
has said, correctly, that a session with no plan wastes his night. **Never mention
the hour and never tell him to sleep. Never start voice** — text only until he
types the literal word "voice". His dictation garbles words; use context, never
quiz him on one odd word ("hellman" = Helmion).

When he asks whether something is real, **trace it and answer with the citation**
— that is the question he most wants answered honestly, and twice tonight the
answer was "three of these four are real and one is a bug."
