# SESSION BOARD

Live registry of agent sessions working in this repo. **Add a row when you
start; set status `done` when you finish.** Claim files BEFORE editing them;
if a file you need is claimed by an `active` row, do not touch it.

This board exists because two prior sessions collided inside
`bigsister_neon_log.mjs` and produced "parsedDetail is not defined."

| session_id | repo | branch | scope_paths | claimed_files | started_at | status |
|---|---|---|---|---|---|---|
| f3673e34-e670-4092-8cbd-4c8b3ce23f2d (orchestrator) | E:\Helmion | main | leak fix + orchestration | src/agent/providers.mjs; docs/*; SESSION_BOARD.md | 2026-07-28 12:20 -06:00 | active (reopened ~13:00) |
| f3673e34/agent-A-three-fixes | E:\Helmion | main | custom endpoint wiring + status-label honesty + input clear | desktop/Helmion.Desktop/MainWindow.xaml; desktop/Helmion.Desktop/MainWindow.xaml.cs; desktop/Helmion.Desktop.Core/ConsoleSession.cs; desktop/Helmion.Desktop.Core/ProviderProfiles.cs; desktop/Helmion.Desktop.Core/DesktopSettings.cs; desktop/Helmion.Desktop.Core/CustomChatSession.cs (new); desktop/Helmion.Desktop.Core/AgentBridge.cs; desktop/Helmion.Desktop.Core/PilotSnapshot.cs; desktop/Helmion.Desktop.SmokeTests/Program.cs; src/agent/env.mjs; src/agent/providers.mjs (extend only, after orchestrator's redaction edit); src/agent/loop.mjs; src/agent/bridge.mjs; src/agent/session.mjs; test/custom-provider.test.mjs (new); test-support/openai-compatible-stub.mjs (new) | 2026-07-28 ~13:05 -06:00 | done (2026-07-28 ~14:35 -06:00) — build 0W/0E, 7 smoke suites pass, npm test 93 pass 0 fail |
| f3673e34/agent-B-voice-phase1 | E:\Helmion | main | Whisper+Kokoro engines behind extracted interface; SAPI deletion; NO MainWindow edits | DELETED Core/SpeechEngine.cs + Core/AudioEndpointProbe.cs; NEW Core/{ISpeechEngine,LocalVoiceEngine,WhisperSpeechRecognizer,KokoroSpeechSynthesizer,SpeechTextCleaner,DictationCommands,VoiceModelPaths,VoiceAudio}.cs; EDITED Core/VoiceSession.cs (internals only — public surface intact), Core/Helmion.Desktop.Core.csproj, .gitignore; NEW SmokeTests/VoiceSmokeChecks.cs + one call line in SmokeTests/Program.cs (shared with agent-A); NEW desktop/scripts/get-voice-models.ps1, docs/VOICE_PHASE1_WIRING_TODO.md; desktop/models/ (gitignored) | 2026-07-28 ~13:05 -06:00 | done (2026-07-28 ~14:40 -06:00) — build 0W/0E; 39 voice checks + Kokoro→Whisper round-trip PASS; MainWindow never touched |
| f3673e34/agent-E-flywheel-audit | E:\Helmion | main | READ-ONLY forensic audit of the self-improvement stack (hooks, md layer, learnings pipeline, Neon lanes, Helmion MCP servers, advisory lane) | docs/FLYWHEEL_AUDIT_2026-07-28.md (NEW — only file written); SESSION_BOARD.md (this row only). Read-only everywhere else; two SELECT-only Neon probes left in session scratchpad | 2026-07-28 ~15:45 -06:00 | done (2026-07-28 ~16:35 -06:00) — 21 findings; headline: ProfileSyncEngine.cs:196-208 truncates ~/.claude/LEARNINGS.md + LESSONS.md, wiping distill.mjs output |
| unknown (concurrent, unregistered) | E:\Helmion | main | src/agent/ (secret redaction per SECURITY_FIX_REPORT.md); deleted fix-voice-and-github.ps1 | src/agent/redact.mjs; src/agent/tools.mjs; SECURITY_FIX_REPORT.md | 2026-07-28 ~12:43 -06:00 (observed) | active (observed 12:43) |
| f3673e34/agent-D-workspace-bug | E:\Helmion | main | agent turn ran with workspace C:\Users\troyh\.grok — tool workspace must always be the registered workspace, never a provider CLI home dir | desktop/Helmion.Desktop.Core/EnvironmentSettingsStore.cs; desktop/Helmion.Desktop.Core/AgentWorkspaceResolver.cs (NEW); desktop/Helmion.Desktop/MainWindow.xaml.cs (ResolveAgentWorkspace only — agent-A row is done); desktop/Helmion.Desktop.SmokeTests/Program.cs (append one suite). NOT touching: .env, Kokoro/Whisper/VoiceAudio, src/agent/redact.mjs, src/agent/tools.mjs, SECURITY_FIX_REPORT.md | 2026-07-28 ~15:45 -06:00 | done (2026-07-28 ~16:10 -06:00) — root cause: `.env:22` WORKSPACE_PATH=C:\Users\troyh\.grok passed the drive-root-only guard; build 0W/0E, 8 smoke suites (7 baseline + new 10-check workspace guard) + voice 39 + round-trip pass, npm test 122 pass 0 fail |
| f3673e34/agent-F-agent-os-package | E:\Helmion | main | one-button ANONYMIZED agent-OS installer (self-improvement loop file set + hooks) for Claude Code / Codex / Gemini CLI | src/core/agent-os.mjs (NEW); templates/agent-os/** (NEW tree); test/agent-os.test.mjs (NEW); bin/helmion.mjs (ADD `agent-os` subcommand + help text ONLY — no edits to existing command bodies); README.md (APPEND "Agent OS" section only); package.json (2 surgical adds: "templates" into `files`, agent-os.mjs into `check` — without the first, templates would not ship). NOT touching: desktop/**, src/agent/**, .env, SECURITY_FIX_REPORT.md, hooks/pretooluse.ps1, any other row's claimed files | 2026-07-28 ~16:15 -06:00 | done (2026-07-28 ~16:55 -06:00) — npm test 145 pass 0 fail (122 baseline + 23 new); all 3 targets installed to scratch and both hooks exercised end-to-end; denylist positive-controlled (FAILs on poisoned template, PASSes clean); 4 real defects found and fixed during proof |
| f3673e34/agent-G-flywheel-repair | E:\Helmion | main | repair the broken half of the flywheel per docs/FLYWHEEL_AUDIT_2026-07-28.md findings 2,3,4,5 + JSON.parse silent-write-loss | src/core/advisory-action.mjs (NEW); src/core/advisory-lane.mjs (NEW); src/adapters/neon.mjs (registerGovernanceAction NEW + insertGovernanceAction helper + recordReview/getConsensus hardened); src/mcp/server.mjs (advisory toolset + helmion_register_action); test/advisory-consensus-gate.test.mjs (NEW); test/advisory-lane.test.mjs (NEW); bin/helmion.mjs (ADD `advisory` subcommand + help block ONLY — shared add-only with agent-F, no edits to existing command bodies); docs/FLYWHEEL_AUDIT_2026-07-28.md (APPENDED section 6 only, .bak taken, agent-E row was done); C:\Users\troyh\.claude\scripts\neon\bigsister_neon_log.mjs (JSON.parse guard, .bak first); C:\Users\troyh\.claude\scripts\neon\session_end_detail.mjs (NEW); C:\Users\troyh\.claude\scripts\session_end_safe.sh (.bak first, additive). NOT WRITTEN: sql/bigsister/001_advisory_output_review_state.sql — BLOCKED by hook_autonomy_boundary.ps1 as Tier B, not bypassed. NOT touching: CLAUDE.md, settings.json, LESSONS.md, LEARNINGS.md, MEMORY.md, memory/*, .env, ~/.claude.json, ClaudeProfileInstaller.cs, ProfileSyncEngine.cs, Kokoro*/Whisper*/VoiceAudio.cs/ISpeechEngine.cs, src/agent/** | 2026-07-28 ~16:40 -06:00 | done (2026-07-28 ~17:05 -06:00) — build 0W/0E, 8 smoke suites + voice 39 + Kokoro→Whisper round-trip pass, npm test 170 pass 0 fail (no baseline test lost). Findings 3/2/13/11 fixed, each with a positive control (consensus gate: 4 FAIL before → 7 PASS after; JSON.parse: exit 0 silent → exit 2 loud; agent_logs 287/289 NULL detail → #395/#396 populated). Finding 4 = written plan in audit §6.4; nothing deleted, CLAUDE.md untouched. 🔴 OPEN: bigsister review-state DDL needs Troy (agent_logs#394, text in audit §6.5). 🔴 NEW finding: ~/.claude/LESSONS.md is write-only too — distill.mjs:36 writes it, nothing injects it (audit §6.3) |
| f3673e34/agent-H-lessons-topic-split | C:\Users\troyh\.claude (not a git repo) | n/a | split LESSONS.md into topic files + pointer-only session start + UserPromptSubmit topic-detection hook | C:\Users\troyh\.claude\lessons\INDEX.md; C:\Users\troyh\.claude\lessons\lessons-*.md (NEW); C:\Users\troyh\.claude\scripts\session_start_inject.sh (.bak first, lessons section only); C:\Users\troyh\.claude\scripts\hook_topic_lessons.ps1 (NEW); C:\Users\troyh\.claude\settings.json (.bak first, ADDITIVE 4th UserPromptSubmit hook only — existing 3 untouched); SESSION_BOARD.md (this row only). NOT touching: LESSONS.md (append-only intake for distill.mjs:36 + dedup source at :199), CLAUDE.md, MEMORY.md, memory/*, the 7 pre-existing lessons/*-documentation.md + popup-playtest-history.md file CONTENTS, any other row's claimed files, E:\Helmion source | 2026-07-28 ~17:50 -06:00 | done (2026-07-28 ~18:05 -06:00) — 11 topic files created; all 8 LESSONS.md entries routed exactly once (verified by SOURCE-line count 8=8), LESSONS.md byte-identical at 7,799. session_start_inject.sh exit 0, lessons section 4,167→1,474 bytes, total 29,202→26,509. New hook_topic_lessons.ps1 registered as UserPromptSubmit[3], existing 3 intact, settings.json parses. 10 hook tests all exit 0: unity→only lessons-unity.md 1,715B; voice swap→only lessons-voice.md 2,623B; no-match→0B; malformed/empty/no-prompt stdin→0B; de-dup suppresses repeat, re-injects on topic switch; 5-topic prompt capped at 2. Positive control (route→missing file) exits 0, injects nothing, does not poison de-dup state; INDEX.md restored hash-identical. 🔴 SCOPE NOTE: 25,035 of the remaining 26,509 bytes is the LEARNINGS tail-50 + global LEARNINGS blocks, NOT lessons — out of my scope, flagged to main, unfixed |
| f3673e34/agent-C-model-switcher | E:\Helmion | main | per-task model auto-switcher (tier router for all providers) | src/agent/model-router.mjs (NEW); src/agent/loop.mjs; src/agent/session.mjs; src/agent/env.mjs; src/agent/bridge.mjs; test/model-router.test.mjs (NEW). NOT touching: src/agent/providers.mjs (orchestrator-claimed — router passes model in via existing `model` param, no edit needed), redact.mjs, tools.mjs, desktop/** | 2026-07-28 ~14:45 -06:00 | done (2026-07-28 ~15:40 -06:00) — npm test 122 pass 0 fail (93 baseline + 29 new); stub proof shows 3 distinct models chosen; live xAI run switched grok-4.3→grok-4.5 |
| f3673e34/agent-J-ask-permission-mode | E:\Helmion | main | fourth permission tier "ask" (ALWAYS ASK) — every tool eligible, but each individual call must be approved by a human before it executes; fail-closed on missing approver, timeout, malformed response and non-TTY | desktop/Helmion.Desktop.Core/AgentPermission.cs; desktop/Helmion.Desktop.Core/AgentBridge.cs; desktop/Helmion.Desktop/MainWindow.xaml; desktop/Helmion.Desktop/MainWindow.xaml.cs; desktop/Helmion.Desktop.SmokeTests/Program.cs (APPEND one suite call only); desktop/Helmion.Desktop.SmokeTests/AskPermissionSmokeChecks.cs (NEW); src/agent/approval.mjs (NEW); src/agent/tools.mjs (verified clean at HEAD 1bdd392 — the unregistered row's redaction edits are already committed, nothing uncommitted to collide with); src/agent/bridge.mjs; src/agent/session.mjs; test/ask-permission.test.mjs (NEW). NOT touching: package.json (agent-I is ACTIVE and owns the `check` list — new src/agent/approval.mjs deliberately NOT added there, flagged as follow-up), src/core/mcp-install*, test/mcp-install-security.test.mjs, test-support/**, .env, src/agent/redact.mjs, ~/.claude*. AMENDED MID-TASK: src/agent/loop.mjs WAS edited after all (2 lines in resetSessionState) — it forwarded only permissionMode and dropped every other option, silently detaching the ask approver on each reconfigure; no active row claimed it (agent-A/agent-C both done) | 2026-07-28 ~18:10 -06:00 | done (2026-07-28 ~19:05 -06:00) — build 0W/0E; 9 smoke suites (8 baseline + new ask-permission 58 checks) + voice 39 + Kokoro→Whisper round-trip pass; npm test 292 pass 0 fail, 31 of them mine (baseline 171 provably intact — `git diff HEAD -- test/` is empty; the other +90 are agent-I's 41 and the unregistered plugins/slash-commands session's 49). Positive control vs the pre-change tools.mjs from HEAD: 2 of 3 assertions FAIL there (ask exposed 0 tools; an APPROVED write never landed) and 0 fail after; a bypass control shows full mode ignores the very same deny-approver. Fail-closed proven by side effect on disk for: no approver, timeout, thrown approver, garbled answer, non-TTY, unknown/duplicate/forged id, shutdown, and 3 fake auto-approve env vars. 🔴 NOT DONE: src/agent/approval.mjs is NOT in package.json `check` (agent-I owns that file — left alone deliberately, needs a one-line add later). 🔴 UI buttons were compiled and code-read only, never clicked. ROUND 2 (2026-07-28 ~19:45, agent-L's two referred defects): ADDED test/bridge-env-refresh.test.mjs (NEW) + src/agent/bridge.mjs stale-key fix — refreshProviderKeyFromEnv() re-reads .env each turn and swaps the provider ONLY when the resolved key really changed (calling reconfigure() unconditionally would hit resetSessionState and wipe history every message); also tracks customProvidersFromDesktop so a desktop-managed endpoint list stays authoritative over .env. Positive control: same 2-turn scenario against a copy of the current bridge with only the refresh call stripped → turn 2 still sends `Bearer stale-key-001`; with the fix → `Bearer fixed-key-002`. Final gate: clean --no-incremental build 0W/0E, 9 smoke suites + voice 39 + round-trip pass, npm test 294 pass 0 fail. 🔴 DECLINED agent-L's paste-lock item — evidence against it: paste is handled by DataObject.AddPastingHandler (MainWindow.xaml.cs:619 → ConsoleInputBox_OnPaste:1418), NOT by PreviewKeyDown, so the stated mechanism does not apply; sending is ALREADY guarded at :1466; the composer is cleared at DISPATCH (:1484) and :1670 deliberately preserves text entered during a turn. Blocking paste would remove the ability to compose your next message while the agent works. Needs a named harm before I take that capability away |
| f3673e34/agent-L-gemini-doc-verify | E:\Helmion | main | READ-MOSTLY verification of D:\_Docs\helmion-gemini-parse-for-claude.md open-item claims (Helmion C# §2, voice/UI §3, hook gates §5 read-only, SiteVector §4 read-only) | docs/GEMINI_DOC_VERIFICATION_2026-07-28.md (NEW — primary write); SESSION_BOARD.md (this row only). NOT touching: MainWindow.xaml.cs / AgentPermission.cs / AgentBridge.cs / src/agent/tools.mjs / src/agent/bridge.mjs / src/agent/session.mjs (agent-J ACTIVE); src/core/mcp-* / bin/helmion.mjs / package.json (agent-I ACTIVE); C:\Users\troyh\.claude\** (READ-ONLY, hooks not edited); .env; ~/.claude.json; C:\Users\troyh\sitevector (READ-ONLY) | 2026-07-28 ~18:15 -06:00 | done (2026-07-28 ~18:55 -06:00) — NO code changed; only docs/GEMINI_DOC_VERIFICATION_2026-07-28.md + this row. 15 items verified against code: 9 VERIFIED-DONE, 3 GENUINELY-OPEN, 4 PARTIAL. Headline: §3.1 401 key reload is PARTIAL — C# reloads .env per submit (MainWindow.xaml.cs:1418) but AgentBridge.EnsureStartedAsync:46-52 reuses the live Node child and TurnAsync:147-157 never sends `configure`, so bridge.mjs:221 skips loadHelmionEnv when a stale-but-non-empty key is cached → a mid-session .env key fix still 401s. §3.4 paste lock GENUINELY-OPEN (PreviewKeyDown:1299-1302 ignores everything but Enter). BOTH fixes left UNAPPLIED — bridge.mjs + MainWindow.xaml.cs are agent-J ACTIVE; exact changes written in the doc. §2 all four VERIFIED-DONE (ToolDispatcher real but off the live path; the Node bridge does the real tool calling). §5.4 TROY-APPROVED gap CLOSED (hook_autonomy_boundary.ps1:189 includes new_string, probe-proven 2→0). §5.3 doc claim FALSE: literal scan is deliberately receiver-agnostic (:164-173). SiteVector §4.1/§4.2 genuinely open, not fixed per instruction. Gates: build 0W/0E, all smoke suites pass (23+9+58+39 + round-trip), npm test 275 pass 0 fail (stated baseline 171 is stale, nothing failing) |
| f3673e34/agent-M-moshi-duplex | E:\Helmion | main | three-backend voice switch (exactly one live at a time, Moshi default w/ automatic Whisper+Kokoro fallback) + empirical Moshi install attempt | desktop/Helmion.Desktop.Core/VoiceBackend.cs (NEW); desktop/Helmion.Desktop.Core/IDuplexVoiceSession.cs (NEW); desktop/Helmion.Desktop.Core/MoshiDuplexSession.cs (NEW); desktop/Helmion.Desktop.Core/MoshiAvailabilityProbe.cs (NEW); desktop/Helmion.Desktop.Core/VoiceBackendSelector.cs (NEW); desktop/Helmion.Desktop.SmokeTests/VoiceBackendSmokeChecks.cs (NEW); desktop/Helmion.Desktop.SmokeTests/Program.cs (APPEND one suite call only); docs/MOSHI_INSTALL_ATTEMPT_2026-07-28.md (NEW); SESSION_BOARD.md (this row only). NOT touching: ISpeechEngine.cs, LocalVoiceEngine.cs, VoiceSession.cs (public surface FROZEN — selector sits ABOVE it, no edit needed), Whisper*/Kokoro*/VoiceAudio/SpeechTextCleaner, MainWindow.xaml{,.cs}, .env, ~/.claude.json, src/agent/redact.mjs, src/agent/**, package.json | 2026-07-28 ~19:50 -06:00 | KILLED mid-run (console windows surfaced on Troy's screen) — file claim RELEASED, superseded by f3673e34/agent-M3-moshi-silent below. Left on disk: VoiceBackend.cs, IDuplexVoiceSession.cs, MoshiAvailabilityProbe.cs, VoiceBackendSelector.cs, VoiceBackendSmokeChecks.cs + the call at SmokeTests/Program.cs:786. NEVER written: MoshiDuplexSession.cs, docs/MOSHI_INSTALL_ATTEMPT_2026-07-28.md |
| f3673e34/agent-M3-moshi-silent | E:\Helmion | main | takes over row above: finish/verify the three-backend voice switch + empirical Moshi-on-this-hardware verdict, all work SILENT (detached-WMI + logfile polling, zero console windows) | INHERITED from the killed agent-M row, now claimed here: desktop/Helmion.Desktop.Core/VoiceBackend.cs; desktop/Helmion.Desktop.Core/IDuplexVoiceSession.cs; desktop/Helmion.Desktop.Core/MoshiAvailabilityProbe.cs; desktop/Helmion.Desktop.Core/VoiceBackendSelector.cs; desktop/Helmion.Desktop.SmokeTests/VoiceBackendSmokeChecks.cs; desktop/Helmion.Desktop.SmokeTests/Program.cs (the one existing VoiceBackendSmokeChecks.Run() call line only). NEW: docs/MOSHI_INSTALL_ATTEMPT_2026-07-28.md; E:\Helmion\artifacts\moshi-*.log (scratch). SESSION_BOARD.md (this row + the status cell of the killed agent-M row). NOT touching: ISpeechEngine.cs, LocalVoiceEngine.cs, VoiceSession.cs (public surface FROZEN), Whisper*/Kokoro*/VoiceAudio/SpeechTextCleaner, MainWindow.xaml{,.cs}, .env, ~/.claude.json, src/agent/redact.mjs, src/agent/**, package.json, any other row's claimed files | 2026-07-28 ~20:10 -06:00 | done (2026-07-28 ~19:50 -06:00) — NO C# was written this session; the killed agent-M row's 5 files were already on disk and I VERIFIED them rather than rewriting. Gates: build 0W/0E both projects; 10 smoke suite lines (9 baseline + voice backend selector 56 checks) incl. voice 39 + Kokoro→Whisper round-trip (156,046 B WAV, transcript exact); npm test 298 pass 0 fail (baseline held exactly). MOSHI VERDICT — measured, not reasoned: PyTorch bf16 HARD-BLOCKED, real pasted CUDA OOM ("Tried to allocate 14.32 GiB. GPU 0 has a total capacity of 6.00 GiB"). moshi 0.2.13 + torch 2.13.0+cu126 DID install clean on uv-managed Python 3.12.13. 🔴 TWO BRIEFING CLAIMS DISPROVEN BY MEASUREMENT: (1) "bf16 unsupported on TU116" is FALSE — torch.cuda.is_bf16_supported()=True and a bf16 matmul ran (checksum 262144.0); (2) "7.61 GiB int8 cannot be allocated on a 6 GiB card" is FALSE — it ALLOCATED OK via Windows WDDM system-RAM spill. Real blocker is PCIe spill bandwidth (~2 GiB resident in host RAM), not dtype and not allocation refusal. 🔴 NOT MEASURED, flagged as reasoning not fact: no token rate / latency for the int8 path — I did NOT download the 8.55 GB checkpoint, install rustup, install the CUDA toolkit, or build moshi-backend (~16 GB + hours to confirm a result that only gets worse). 🔴 NO MoshiDuplexSession.cs — no stub, mock or fake adapter written; duplexFactory:null is the honest shape and is the case the suite proves. 🔴 OPEN SEAM: the selector is NOT wired into the running app — MainWindow.xaml.cs:594 still constructs `new VoiceSession()` directly and nothing references VoiceBackendSelector; deliberately not rewired (zero functional gain today since Moshi cannot run, and no human was available to test live audio). 🔴 PRE-EXISTING, NOT MINE: ~/.claude/LEARNINGS.md (361 B) and LESSONS.md (612 B) were ALREADY truncated before I started (agent-H recorded LESSONS.md at 7,799 B); I backed both up and proved byte-identical hashes before/after my smoke runs. Artifacts left at E:\Helmion\artifacts\moshi-venv (~5 GB, disposable). Full writeup: docs/MOSHI_INSTALL_ATTEMPT_2026-07-28.md |
| f3673e34/agent-I-mcp-install-security | E:\Helmion | main | 5-stage gated pipeline for auto-discovering + installing third-party MCP servers from GitHub without blindly trusting them (discovery → code read → sandbox → human approval → permanent log + drift detection vs a recorded baseline) | src/core/mcp-discovery.mjs (NEW); src/core/mcp-code-audit.mjs (NEW); src/core/mcp-sandbox.mjs (NEW); src/core/mcp-sandbox-observer.mjs (NEW); src/core/mcp-approval.mjs (NEW); src/core/mcp-install-ledger.mjs (NEW); src/core/mcp-install-pipeline.mjs (NEW); test-support/mcp-candidates/** (NEW — deliberately poisoned + benign fake servers, only ever executed inside the sandbox harness); test/mcp-install-security.test.mjs (NEW); sql/004_mcp_install_security.sql (NEW, only if the autonomy hook permits — else recorded as blocked, not bypassed); bin/helmion.mjs (ADD `mcp-install` subcommand + help block ONLY — add-only, shared convention with agent-F/agent-G, no edits to existing command bodies); package.json (ADD new src files into `check` only). NOT touching: .env, ~/.claude.json, src/agent/redact.mjs, src/agent/**, desktop/**, CLAUDE.md, settings.json, MEMORY.md, LESSONS.md, hooks/pretooluse.ps1, any other row's claimed files | 2026-07-28 ~17:55 -06:00 | done (2026-07-28 ~18:40 -06:00) — npm test 212 pass 0 fail (171 baseline + 41 new, no baseline test lost); npm run check exit 0. sql/004 NOT written and NOT needed: the ledger reuses helmion.telemetry_events (sql/002_maestro_phase_one.sql:98-106) via recordTelemetry, so there is no DDL and no hook block. Both directions proven: poisoned fixture BLOCKED (8 critical/3 high/2 medium, every flag file:line-cited) vs benign CLEAN (0 critical/high/medium). Sandbox: poisoned MISBEHAVED on all 6 channels + 1 canary exfil attempt, benign clean with its one call to its declared host. Non-TTY CLI refuses with exit 2 and installs nothing even when fed "yes". 2 REAL DEFECTS found and fixed during proof: (1) comment-stripper blanked string interiors so `import {execSync} from 'node:child_process'` was NEVER flagged; (2) the observer's own audit log was blocked by the observer's own fs guard — poisoned server reported as "no-observed-misbehaviour" with 0 events; both now regression-tested. 🔴 UNVERIFIED: the live Neon write path is proven only against an injected store (call contract pinned to src/adapters/neon.mjs:1076), never against the real database — deliberately, no live DB write was made. 🔴 FOLLOW-UP for whoever owns package.json next: agent-J (src/agent/approval.mjs) and agent-K (src/agent/commands.mjs, src/agent/plugins.mjs) both left their new files OUT of the `check` list because this row owned it |
| f3673e34/agent-K-commands-plugins | E:\Helmion | main | user-defined slash commands (project + user dirs, frontmatter, $ARGUMENTS/$N, subdirectory namespacing) + plugin/connector loading whose MCP declarations are REFUSED unless agent-I's install ledger already approved them | src/agent/commands.mjs (NEW); src/agent/plugins.mjs (NEW); test/slash-commands.test.mjs (NEW); test/plugins.test.mjs (NEW); test-support/command-fixtures/** (NEW); bin/helmion.mjs (ADD `plugin` subcommand + help block ONLY — add-only, same convention as agent-F/G/I, no edits to existing command bodies); src/agent/session.mjs (ADD-ONLY: one dispatch branch + /help listing — agent-J ACTIVE on this file, re-read immediately before each edit, no edits to agent-J's approval/permission lines); src/agent/bridge.mjs (ADD-ONLY: `commands` + `expand` cmd branches — agent-J ACTIVE, same rule). NOT touching: package.json (agent-I owns the `check` list — new files deliberately NOT added there, flagged as follow-up), src/core/mcp-* (agent-I's, READ-ONLY consumer), src/agent/approval.mjs, src/agent/tools.mjs, src/agent/redact.mjs, .env, desktop/**, ~/.claude* | 2026-07-28 ~18:20 -06:00 | done (2026-07-28 ~19:15 -06:00) — npm test 294 pass 0 fail (210 measured baseline at my start + 49 mine + concurrent agents' additions; no baseline test lost), npm run check exit 0. No C# touched, so no dotnet build needed. Slash commands: 32 tests, format implemented against https://code.claude.com/docs/en/skills quoted verbatim in the module header. Plugins: 17 tests. SECURITY proven both directions with a real positive control against agent-I's own ledger: same .mcp.json REFUSED with no baseline → APPROVED after saveBaseline() → REFUSED again after the server code changed; npx/http declarations refused as unvettable; a throwing ledger fails CLOSED. `plugin add` of a remote source refused with exit 2, no fetch path exists in the file. 3 REAL DEFECTS found by my own tests and fixed: (1) `argument-hint: [env] [tag]` was parsed as a YAML list and collapsed to "env" — inline-list parsing now restricted to LIST_FIELDS; (2)+(3) two test-expectation bugs of my own (namespace shape, Windows-relative path) corrected, not papered over. ⚠️ DELIBERATE DIVERGENCE from the assignment: brief asked for "$1..$9" 1-based; the published spec says $0=first, $1=second, and I implemented the SPEC so Helmion matches the tool Troy actually uses. ⚠️ DELIBERATELY NOT IMPLEMENTED: `` !`cmd` `` dynamic context injection — executing shell out of markdown that can arrive from a third-party plugin is the hole this repo is closing; command bodies are inert text. 🔴 HONEST SEAM: Helmion has NO MCP client (grep: mcpServers appears only in src/core/mcp-install-pipeline.mjs), so an `approved` verdict yields a vetted registration RECORD, not a running server; a future client must consume `plugin.registrations` and nothing else. 🔴 FOLLOW-UP: src/agent/commands.mjs + src/agent/plugins.mjs are NOT in package.json `check` (agent-I owned that list). 🔴 UNRELATED LIVE DEFECT FOUND, NOT MINE TO FIX: every OpenAI agent turn with tools 400s — model-router.mjs:61-65 routes to gpt-5.6-luna/terra/sol and OpenAI rejects function tools + reasoning_effort on /v1/chat/completions for those models ("use /v1/responses or set reasoning_effort to 'none'"); reasoning_effort appears nowhere in Helmion source, so it is the model default |
| f3673e34/agent-N-completeness-audit | E:\Helmion | main | READ-ONLY completeness audit of every directive Troy issued 2026-07-28 (Helmion core, voice, flywheel, handoff addendum, known open bugs) | docs/COMPLETENESS_AUDIT_2026-07-28.md (NEW — only file written); SESSION_BOARD.md (this row only). Read-only everywhere else incl. C:\Users\troyh\.claude, D:\_Docs, C:\Users\troyh\sitevector. NOT touching: any source file, .env, ~/.claude.json | 2026-07-28 ~20:00 -06:00 | done (2026-07-28 ~20:55 -06:00) — NO code changed; only docs/COMPLETENESS_AUDIT_2026-07-28.md + this row. 28 directives + 4 known bugs audited against CODE via 7 parallel read-only agents plus 5 direct spot-checks by me (BASE_RULES/LEARNINGS bytes, the slash hijack, GlobalDataBadgeText, AgentBridgeEvent, new VoiceSession at :594, redact.mjs:9, hook regex, settings.json hook registration). Result: 12 DONE-VERIFIED, 9 PARTIAL/UNWIRED, 8 NOT-STARTED, 2 BLOCKED, 4 still-open bugs. HEADLINE: the systematic failure is REACHABILITY, not quality — 5 complete, green, tested features are unreachable from the app Troy actually opens. 🔴 DATA LOSS UNRECOVERED: ~/.claude/BASE_RULES.md (1,635 B) and LEARNINGS.md (361 B) are byte-identical to ClaudeProfileInstaller.cs:185/:218 templates; ~3,877 B of Troy's supreme-rule file gone, no on-disk copy survives anywhere I can reach. 🔴 MainWindow.xaml.cs:1488 routes every /-prefixed input to PowerShell, hijacking the entire slash-command engine. 🔴 hook_block_commit_qa.ps1:117 fails BOTH ways (quoted path blocks a good commit; -F - allows one with zero QA). 4 OTHER AGENTS' CLAIMS REFUTED with cites, incl. agent-K's OpenAI-400 (providers.mjs:134-136 already fixes it, 4 tests pass) |
| f3673e34/agent-O-local-model-routing | E:\Helmion | main | install a LOCAL model (Ollama) on this box + route cheap Helmion turns to it through the EXISTING custom-endpoint path; add a `local` tier to the router with explicit testable trivial-vs-hard criteria + safety carve-outs | src/agent/model-router.mjs (EXTEND — agent-C row is done); src/agent/local-provider.mjs (NEW); src/agent/loop.mjs (AMENDED MID-TASK: local provider swap + frontier fallback must live at the chatWithTools call site; agent-C/agent-J both DONE, no active row claims it); test/local-routing.test.mjs (NEW); docs/LOCAL_MODEL_ROUTING.md (NEW); E:\Helmion\artifacts\ollama-*.log + probe-*.mjs (scratch). SESSION_BOARD.md (this row only). NOT touching: .env, ~/.claude.json, src/agent/redact.mjs, docs/COMPLETENESS_AUDIT_2026-07-28.md (agent-N ACTIVE), desktop/**, src/agent/providers.mjs (no edit needed — `custom` already routes through openAiCompatibleTurn at providers.mjs:57-69), package.json, any other row's claimed files | 2026-07-28 ~20:30 -06:00 | done (2026-07-28 ~21:20 -06:00) — npm test 338 pass 0 fail (298 baseline provably intact + my 22 + 18 added by concurrent sessions), npm run check exit 0. No C# touched. Ollama v0.32.5 installed from the official ZIP (size-verified 1,457,824,795 B) to %LOCALAPPDATA%\Programs\Ollama; models on E:\ollama-models (C: has 23.6 GB free). NO-SPILL PROOF: `ollama ps` PROCESSOR=**100% GPU**, /api/ps size_vram==size==3,202,676,161 B exactly, 57.9 tok/s; ctx 8192 configured (374 MiB headroom, 57.3 tok/s); 16384 also fits but 43.6 tok/s. Bound 127.0.0.1 ONLY (verified by socket, not assumption). 🔴 MODEL NAME CORRECTED, NOT SUBSTITUTED SILENTLY: "Qwen 3.5 7B" does not exist — Qwen3.5 small line is 0.8B/2B/4B/9B; 9b=6.6 GB cannot fit a 6.00 GiB card, so qwen3.5:4b Q4_K_M (reports 4.7B). 🔴 2 REAL DEFECTS found by my own tests and fixed: (1) `\b(...\|\\.env\|...)\b` made `.env` UNMATCHABLE — "read the .env" routed LOCAL; (2) default-ON silently hijacked 3 concurrent bridge tests because real Ollama was listening → now OPT-IN (HELMION_LOCAL_ENABLED=1). 🔴 providers.mjs NOT edited (orchestrator ACTIVE) — but my brief's premise is FALSE by measurement: Ollama ACCEPTS reasoning_effort and merely IGNORES tool_choice. qwen3.5:4b is a REASONING model: 2603 tokens/~40 s for a 188-char answer vs 30 tokens/0.9 s with reasoning_effort='none'. Referred to main; until it lands local routing is a NET LOSS and docs say leave it OFF. 🔴 UNVERIFIED: multi-ROUND tool chains (only first action, 3/3), behaviour under GPU contention (Unity/Blender will break the no-spill result), other OpenAI-compatible runtimes. Server left RUNNING hidden (PID 34980, parent cmd 13776, MainWindowHandle=0); stop with `Get-Process ollama \| Stop-Process`; does NOT autostart at login. Full writeup: docs/LOCAL_MODEL_ROUTING.md. ROUND 2 (2026-07-28 ~22:00, after main landed the providers.mjs `reasoningEffortNone` param — verified at providers.mjs:63/82/161, not taken on trust): wired the flag through loop.mjs for the LOCAL path ONLY (`reasoningEffortNone: Boolean(choice.isLocal)`, never inferred from providerId 'custom', so LM Studio/vLLM/DeepSeek are untouched); 2 new tests assert BOTH directions (local body carries `reasoning_effort:'none'`; a non-local custom endpoint has the key ABSENT, not merely falsy). Gate: npm test 340 pass 0 fail, npm run check exit 0. 🔴 I CORRECTED MY OWN EARLIER CLAIM: the 0.9 s figure was a bare-API best case and is NOT what a Helmion turn costs. Measured end-to-end through the real router (10 warm turns + 2 cold starts): cold 9–18 s, warm median ~5 s, warm range 2.2–33 s, vs ~1–3 s from a frontier fast tier — local is SLOWER on the median and worse on the tail; what it buys is zero API spend and locality. Remaining latency is OUTPUT LENGTH, not thinking (~57 tok/s: 235 chars=2.2 s, 2050 chars=10.4 s, 2908 chars=33 s). LOCAL_DEFAULT_TIMEOUT_MS STAYS 45000 — dropping to 20000 as I originally wrote would abort the slow tail and cause a pointless double wait. NEXT LEVER (proposed, NOT done — deliberate prompt-behaviour change): cap local answer length, which would put trivial turns near 1–2 s |

| f3673e34/agent-Q-wire-the-ui | E:\Helmion | main | WIRE THE 5 UNREACHABLE FEATURES into the Pilot app: slash commands reachable, dictate mode, model-switcher display, MCP install security surface (audit/discover only — approval stays a real human gate), voice backend selector; plus dead-mic watchdog + composer/focus bugs | desktop/Helmion.Desktop/MainWindow.xaml; desktop/Helmion.Desktop/MainWindow.xaml.cs; desktop/Helmion.Desktop.Core/AgentBridge.cs; desktop/Helmion.Desktop.Core/WhisperSpeechRecognizer.cs; desktop/Helmion.Desktop.Core/VoiceBackendSelector.cs; desktop/Helmion.Desktop.SmokeTests/*.cs | 2026-07-28 21:20 -06:00 | superseded — interrupted before gates were run; work left on disk UNVERIFIED. Taken over and verified by f3673e34/agent-Q2-wire-the-ui below. |

| f3673e34/agent-Q2-wire-the-ui | E:\Helmion | main | TAKES OVER the agent-Q row above: verify + finish wiring the 5 unreachable features into the Pilot (slash commands, dictate mode, model-switcher display, MCP audit/discover surface, voice backend selector) + dead-mic watchdog + composer/focus bugs. VERIFY-FIRST: agent-Q's edits were already on disk; I read every one against code before changing anything. | INHERITED from the agent-Q row: desktop/Helmion.Desktop/MainWindow.xaml; desktop/Helmion.Desktop/MainWindow.xaml.cs; desktop/Helmion.Desktop.Core/AgentBridge.cs; desktop/Helmion.Desktop.Core/WhisperSpeechRecognizer.cs; desktop/Helmion.Desktop.Core/VoiceBackendSelector.cs; desktop/Helmion.Desktop.Core/McpSecurityRunner.cs; desktop/Helmion.Desktop.SmokeTests/ProfileInstallerGuardChecks.cs; SESSION_BOARD.md (these two rows only). NOT touching: .env, ~/.claude.json, src/agent/redact.mjs, src/agent/providers.mjs, src/agent/local-provider.mjs, src/agent/loop.mjs, src/agent/model-router.mjs (agent-O2 ACTIVE), desktop/Helmion.Desktop.Core/ClaudeProfileInstaller.cs + ProfileSyncEngine.cs (agent-P's recovery work) | 2026-07-28 21:20 -06:00 | done (2026-07-28 ~22:05 -06:00) — VERIFY-FIRST outcome: agent-Q had already wired all 5 features + both bugs and was killed BEFORE running a single gate, so none of it was known to compile. I proved it rather than rewriting it. Gates: `dotnet build` 0W/0E on BOTH the WPF app (Helmion.Desktop — where MainWindow's 509-line change lives; the SmokeTests chain does NOT compile it, so building only SmokeTests would have proven nothing) and SmokeTests; 11 smoke suite lines exit 0 incl. profile installer guard 9, voice 39, Kokoro→Whisper round-trip (156,046 B WAV, transcript exact), and "200 start/death/stop races held the single-backend invariant"; npm test 346 pass 0 fail (340 baseline provably held); npm run check exit 0. 🔴 DATA-SAFETY POSITIVE CONTROL: SmokeTests/Program.cs:373 still runs a REAL ProfileSyncEngine sync against the live ~/.claude. I backed up + sha256'd 6 files first, ran the suite TWICE, and all five stable canaries are byte-identical after — BASE_RULES.md still 5,512 B with mtime UNCHANGED at 21:26, i.e. the installer never opened it. The preserve-existing default (ClaudeProfileInstaller.cs:96-100, reached via ProfileSyncEngine.cs:209 passing no override) now holds under the exact live run that destroyed that file twice today. (~/.claude.json is excluded as a canary on purpose: Claude Code rewrites it continuously — it changed 75,099→72,407 B on its own between two of my reads, so it can never be a clean before/after comparison.) 3 FIXES OF MY OWN, all in MainWindow.xaml.cs: (1) real defect — `commands` (bridge.mjs:276) never reads req.workspace and re-scans state.runtime.root (:94), while `turn` (:322) and `configure` (:240) DO adopt it, and the bridge starts at `env.workspace || process.cwd()` (:54); so a listing taken before the first turn can describe a different folder and the unknown-command warning would falsely accuse a command that really exists — the warning is now gated on the listing's workspace matching ResolveAgentWorkspace(); (2) the composer was cleared even when the text arrived as overrideText from VOICE, silently destroying a half-typed draft (the nit COMPLETENESS_AUDIT flagged at item 4) — now clears only when the text came from the box, and dictation's "send it" still clears because it passes no override; (3) recorded _knownCommandsWorkspace to support (1). .BAK A/B RESOLVED, not deleted: live VoiceBackendSelector.cs is byte-identical (18,098 B, modulo CRLF) to the .fixed.bak epoch pair-writer, NOT to the 14,693 B .control.bak — the winner is the fixed one and the 200-race smoke check is its positive control; both .bak files left on disk (they end in .bak so the `**/*.cs` glob never compiles them). 🔴 APPROVAL GATE UNTOUCHED AND STILL CLI-ONLY, by design: McpSecurityRunner surfaces ONLY discover+audit; mcp-approval.mjs:80-89 refuses a non-TTY caller and a WPF app has no TTY, which is what makes the challenge phrase mean something — the panel says so in its own text. 🔴 NOT EXECUTED: I cannot click WPF, so every UI claim here is compile-verified + code-read, never clicked. Troy is the final test. |

| f3673e34/agent-S-grok-voice-research | E:\Helmion | main | RESEARCH ONLY — does Grok Build have native mic/STT, or is a Whisper bridge needed for Grok sessions | docs/GROK_VOICE_RESEARCH_2026-07-28.md (NEW — only file written); SESSION_BOARD.md (this row only). Read-only everywhere else incl. C:\Users\troyh\.grok (auth.json never opened). NOTHING built, installed or configured | 2026-07-28 ~22:10 -06:00 | done (2026-07-28 ~22:35 -06:00) — VERDICT **USE NATIVE**. Grok Build 0.2.112 is installed at C:\Users\troyh\.grok\bin\grok.exe (auth configured) and has built-in voice dictation on Ctrl+Space/F8 — proven from HIS OWN installed copy (CHANGELOG.md:17,36,39,46 + docs/user-guide/21-terminal-support.md:210-228), not from a blog. No Whisper front-end needed for Grok input. xAI STT is api.x.ai/v1/stt at $0.10/hr REST, $0.20/hr streaming (docs.x.ai/developers/pricing). 🔴 AMBIGUOUS, NOT GUESSED: whether CLI dictation bills his subscription or his negative-balance metered API — xAI docs are silent; auth precedence (02-authentication.md:259-260) puts the OAuth session token ABOVE XAI_API_KEY and the CLI docs show no voice-key setup step, so subscription is likelier; settled in 10 s by pressing Ctrl+Space once. 🔴 MISATTRIBUTION CAUGHT: the "needs a separate STT key" claim is from phuryn/grok-build-vscode, a third-party extension that states it is "not affiliated with or endorsed by xAI" — its voice button is the EXTENSION's feature, not the CLI's. 🔴 EVIDENCE DISCARDED: my grep of grok.exe for GROK_VOICE_API_KEY returned "no matches" but the positive control (GROK_VOICE_CAPTURE, proven present) also returned nothing at **0 files searched** — ripgrep skipped the binary; that negative was not used. 🔴 REAL GAP: voice appears in all 22 shipped docs ONLY as dictation (input); there is NO native spoken reply, so Kokoro still has a role on Grok's output. Whisper does not. |
| f3673e34/agent-T-claude-voice-input | E:\Helmion | main | LOCAL VOICE DICTATION for Claude Code (item 6) — hidden hotkey daemon: mic -> Whisper (vocabulary-biased) -> inject text into the focused window. Claude Code ONLY; Grok has native dictation (agent-S verdict), nothing built for it. | tools/voice-dictation/** (ALL NEW: Helmion.VoiceDictation/*, Helmion.VoiceDictation.SelfTest/*, README.md, voice-dictation.config.json); SESSION_BOARD.md (this row only). NOT touching: .env, ~/.claude.json, src/agent/redact.mjs, src/agent/providers.mjs, src/agent/local-provider.mjs, src/agent/loop.mjs, src/agent/model-router.mjs (agent-O2), desktop/** at all (agent-Q2 owns MainWindow/AgentBridge/WhisperSpeechRecognizer/VoiceBackendSelector — I READ WhisperSpeechRecognizer.cs + VoiceAudio.cs + KokoroSpeechSynthesizer.cs and wrote my own standalone copies rather than referencing or editing Core). No new project added to Helmion.Desktop.slnx. | 2026-07-28 ~22:40 -06:00 | done (2026-07-28 ~23:20 -06:00) — commit 2099199. GATES: `dotnet build` 0W/0E on BOTH projects; 31 self-test checks, 0 fail, exit 0. NO Node files touched, so the npm gate is not applicable (git status confirms my diff is tools/voice-dictation/** only). WHAT IT IS: Ctrl+Alt+Space toggles recording, Ctrl+Alt+X discards, text is injected into the focused window but NEVER submitted. 🔴 VOCABULARY PROVEN BY MEASUREMENT, NOT ASSERTION — one Kokoro-synthesized sentence, same model, decoded twice: without the prompt "Halmian runs **Moshe** and **Kakaro** locally, while Cuteye ships duplex LLMs for **dairy forge**" (2/7 terms); with it "**Helmion** runs **Moshi** and **Kokoro** locally, while Qdai ships duplex LLMs for **DairyForge**" (6/7). Moshe→Moshi is the exact mangle from his transcript. 🔴 KYUTAI STILL WRONG ("Cuteye"→"Qdai") — the prompt changed the error without fixing it; documented as unsolved in README rather than buried. 🔴 REAL DEFECT FOUND BY THE TEST, NOT PREDICTED: clipboard was restored 120 ms after Ctrl+V, before a slow target consumed the paste, so the target pasted the PREVIOUS clipboard — now a configurable 400 ms + restore skipped unless our payload is still there. My first harness hid this behind a single-thread artifact (injector's sleep blocked the same pump that had to handle Ctrl+V); the target now runs its own STA message loop, which is what production does. 🔴 NO CONSOLE, PROVEN: PE subsystem byte = 2 (GUI) not 3 (CUI); MainWindowHandle = 0; conhost count 38→38, zero new; stopped by named-event signal not a kill. Both hotkeys registered first try, so nothing else on this box owns them — RegisterHotKey matches modifiers exactly so Grok's plain Ctrl+Space is untouched. 🔴 NOT CLAIMED: Troy speaking into his own mic — the self-test cannot do that, his ears are the final test. 🔴 NOTE FOR WHOEVER OWNS THE TREE: my in-progress sources were swept into someone else's commit bf85fe4 (incl. 336 MB of build output, untracked again in bcd7690 + tools/** added to .gitignore); my own commit 2099199 carries only my paths. Auto-start is OFF by default (autostart-dictation.ps1 -Enable turns it on, HKCU only). |

| f3673e34/agent-R-app-inventory | E:\Helmion | main | READ-ONLY sweep of C:\, D:\, E:\ to inventory every app/product Troy has built: absolute path, one-line description, live URL, evidence-based status, duplicate/worktree map, disk usage | docs/APP_INVENTORY_2026-07-28.md (NEW — only file written); SESSION_BOARD.md (this row only). Read-only everywhere else. NO .env contents read (presence only). NOT touching: any source file, any other row's claimed files, ~/.claude/**, dairyforge.com or any deploy target | 2026-07-28 ~22:45 -06:00 | done (2026-07-28 ~23:25 -06:00) — 60+ trees swept. Status derived from `git log -1 --format=%cI` + `git worktree list` + `git rev-list --count <branch> --not <HEAD>` + live HTTP status codes (curl, 18 URLs). HEADLINE: 4 parent repos own 44 linked worktrees (dairyforge-monorepo=23, gauge-sandbox=8, Caldmere_v2=15, aimforge=2); **76 commits of unmerged work** sit in those worktrees (48 DairyForge + 24 Gauge + 4 AimForge) so mass deletion would destroy real work. 13 df-wt-* dirs have NO git at all (gutted shells, safe to delete). aimforge-main is a WORKTREE of aimforge, not the parent — the name lies. Faith & Light found at C:\Users\troyh\Desktop\Faith-and-Light\code-zips (live https://faithnlight.shop, HTTP 200). E: has only 79 GB free of 931 GB; E:\UnityProjects\_wt alone is 194.9 GB and 149.8 GB of Caldmere snapshots have no git. All deletions listed as SUGGESTION ONLY — nothing deleted, no code changed |

| f3673e34/agent-U-reclaim-disk | E:\UnityProjects (not a git repo at root) | n/a | reclaim ~330 GB on E: — verify-then-delete abandoned Unity snapshot trees per docs/APP_INVENTORY_2026-07-28.md §9 items 1/2/3 | E:\Helmion\artifacts\reclaim-verify.log + reclaim-delete.log (NEW scratch); SESSION_BOARD.md (this row only). DELETION TARGETS (only after per-dir gates pass): E:\UnityProjects\_wt\* except sigil-orbit-land; Caldmere_v2_{chargendiag,SELFTEST,sigils_SELFTEST,QA_Codex}; Caldmere (v1). NEVER touching: E:\UnityProjects\Caldmere_v2 (LIVE game), E:\UnityProjects\_wt\sigil-orbit-land, anything on C: or D:, any source file, any other row's claimed files | 2026-07-28 ~23:30 -06:00 | active |
| f3673e34/agent-V-find-missing-apps | E:\Helmion | main | READ-ONLY second-pass hunt for the apps the first inventory missed — specifically the drive organizer and the voice/Haiku/Google-Docs "diary" app | docs/APP_INVENTORY_2026-07-28.md (**APPEND ONLY** — new "## FOUND LATER" section at the end; agent-R's row is done and its 249 original lines were left byte-for-byte untouched, verified 25,688 → 35,359 bytes with no rewrite); SESSION_BOARD.md (this row only). Read-only everywhere else across C:\, D:\, E:\. NO .env or credential file opened (presence only). NOT touching: any source file, any deploy target, any other row's claimed files | 2026-07-28 ~23:30 -06:00 | done (2026-07-28 ~23:50 -06:00) — DIARY APP FOUND: `C:\Users\troyh\n8n_Pod_Uploader_jarvis\jarvis\api\diary.js` + `jarvis\public\diary.html` — mic (`diary.html:165` getUserMedia/MediaRecorder) → OpenAI Whisper (`diary.js:79`) → **claude-haiku-4-5-20251001** (`diary.js:123`) → structured JSON (`diary.js:100-113`) → Google **Sheets** Apps Script webhook (`diary.js:148-171`). 🔴 TWO CORRECTIONS TO THE BRIEF, not smoothed over: it writes to **Sheets, not Docs** (no Docs call exists in the file) and its prompt is a **construction job-site** diary (`diary.js:101`), not a personal one. The Docs behaviour is a DIFFERENT app — `claude-ai-memory-vault` on his OneDrive Desktop, whose `push_to_docs` really does create Google Docs and which exposes voice-mode GET routes (`vercel.json:7-9`) because MCP connectors do not load in claude.ai voice. 🔴 LIVE STATUS OF THE DIARY UNVERIFIED AND NOT CLAIMED: thinkbuddy.vercel.app returns **401** (Vercel deployment protection) so /diary.html could not be reached; code is complete + committed (`86ddabd` 2026-07-01, html `8b3d24f` 2026-07-20) and routed in jarvis/vercel.json, but reachability is unproven. WHY IT WAS MISSED: it is a second app inside a third app's repo (`n8n_Pod_Uploader_jarvis`) whose package.json is still named `faith-and-thread` and which the inventory lists as "ThinkBuddy". ORGANIZER CONFIRMED: `D:\pc-auto-drive-organizer` IS the one (package.json productName + author "Troy Halter" + "Moves, never deletes"); found a SECOND, earlier one at `D:\_organizer\organize-d.ps1` (PowerShell watcher + scheduled-task installer, same never-deletes promise, has actually run — organize-log.txt present). 7 MORE UNLISTED: aimforge-web (LIVE aimforge-gray.vercel.app 200), caldmere-login (LIVE 200), dairyforge-backend (🔴 ARCHIVE, fly.toml renamed `dairyforge-backend-legacy` 2026-07-25 with an explicit do-not-deploy warning — deploying replaces the LIVE API + runs a stale migration on prod), E:\ShadowbaneServer (Node/socket.io game server), Image Automation (⚠ client_secret.json + token.json in plaintext on the Desktop, NOT opened, worth rotating), OilForge (spec-only 5th vertical, zero code), My project (stray Unity 2022.3.34f1). 🔴 UNRESOLVED, stated not guessed: 24 real voice WAVs at `D:\braindumps_2026-05-21_wav` — I could not identify which app recorded them | 2026-07-28 done |
| f3673e34/agent-O2-local-brevity | E:\Helmion | main | TAKES OVER the agent-O row: make LOCAL model answers BRIEF (instruction, never truncation) and re-measure median + tail through the ACTUAL router | src/agent/local-provider.mjs (LOCAL_BREVITY_INSTRUCTION + withLocalBrevity); src/agent/loop.mjs (one line at the chatWithTools call site — local turns only); test/local-routing.test.mjs (5 brevity tests incl. a frontier byte-identical assertion); docs/LOCAL_MODEL_ROUTING.md (measured numbers); E:\Helmion\artifacts\probe-brevity-latency.{mjs,vbs} + brevity-latency.log (scratch); SESSION_BOARD.md (this row only). NOT touching: .env, ~/.claude.json, src/agent/redact.mjs, src/agent/providers.mjs (orchestrator's — reasoning_effort seam already landed and correct), desktop/** (agent-Q2 ACTIVE in MainWindow/AgentBridge/Whisper/VoiceBackendSelector), package.json, any other row's claimed files | 2026-07-28 ~22:10 -06:00 | done (2026-07-28 ~22:55 -06:00) — npm test 346 pass 0 fail (340 baseline intact, nothing lost), npm run check exit 0. No C# touched; no console window ever surfaced (every probe launched hidden via wscript //B after a WMI-launched child got ^C'd when its launcher exited). CODE WAS ALREADY ON DISK from my interrupted run and was VERIFIED, not rebuilt: LOCAL_BREVITY_INSTRUCTION + withLocalBrevity (local-provider.mjs) applied at loop.mjs:92 on LOCAL turns only; 6 tests incl. "a FRONTIER turn is byte-identical to what it was before brevity existed" and a LEAK GUARD proving brevity never survives into a fallback frontier round. NO max_tokens anywhere — instruction only, model always allowed to finish. 🔴 I REFUTED MY BRIEF'S PREMISE AND MY OWN PRIOR DOC: "a brevity instruction would put trivial turns near 1–2 s" is FALSE. Measured 20 paired warm turns + 4 cold starts through the REAL router (each prompt run BOTH ways in the same run, arm order alternated so neither side wins on prompt-prefix cache): median 3.27→3.12 s (5%, inside noise), p90 8.14→4.82 s, max 9.79→7.70 s, mean answer 680→460 chars, cold 9.65–11.86 s. THIS IS A TAIL FIX, NOT A MEDIAN FIX. ROOT CAUSE measured via Ollama's NATIVE /api/chat (the OpenAI-compatible endpoint hides the split): a warm turn is PROMPT-EVAL-bound, not generation-bound — 678 prompt tokens cost 1.86 s at only ~364 tok/s while a 4-token answer cost 0.05 s; a 3-CHARACTER answer still took 2.23 s. ~2.2 s is a FLOOR no instruction can remove (tool defs ~427 tok ≈ 0.94 s of it; the brevity text is itself ~83 tok ≈ 0.23 s, so it ADDS to the floor it cannot lower). Wording was A/B-TESTED, not guessed: an 87-char variant beat the shipped 385-char one on median (3.13 vs 3.24 s, noise) but LOST on tail (max 4.41 vs 3.75 s) and wrote longer answers (349 vs 294 chars) — the tail is the whole point, so the shipped wording stayed and NO code changed. LOCAL_DEFAULT_TIMEOUT_MS CONFIRMED STAYS 45000. 🔴 RECOMMENDATION: still do NOT enable for speed — frontier fast tier ~1–3 s vs local 3.12 s median / 4.82 s p90 / ~10 s cold; enable only for zero API spend + on-box privacy. 🔴 CAVEAT: qwen3.5:4b does not obey the instruction reliably — one prompt answered in 133 chars WITHOUT brevity came back at 2,613 chars WITH it; did not reproduce on run 2, so model variance, but it is why max is still 7.70 s and why no token cap was added to force it. Corrected the now-false claims in docs/LOCAL_MODEL_ROUTING.md (new §6.1, wrong prediction deliberately left visible above it) and in the local-provider.mjs comments. GPU left as found (model unloaded, VRAM released); ollama PID 34980 left running per instruction. Probes: artifacts/probe-brevity-latency.mjs, probe-local-floor.mjs, probe-brevity-ab.mjs + logs |
| f3673e34/agent-Z-finish-open-items | E:\Helmion | main | two open items: (1) recover ~/.claude/LEARNINGS.md from session transcripts; (2) stop SmokeTests/Program.cs:373 running a REAL ProfileSyncEngine sync against Troy's live machine on every `dotnet run` | desktop/Helmion.Desktop.Core/ProfileSyncEngine.cs; desktop/Helmion.Desktop.SmokeTests/Program.cs; desktop/Helmion.Desktop.SmokeTests/ProfileInstallerGuardChecks.cs (doc comment only); SESSION_BOARD.md (this row only). Read-only outside E:\Helmion. NOT touching: ClaudeProfileInstaller.cs behaviour, .env, ~/.claude.json, any other row's claimed files | 2026-07-29 ~00:10 -06:00 | done (2026-07-29 ~00:45 -06:00) — ITEM 1: **NOTHING TO RECOVER — no data was lost.** Swept every `~/.claude/projects/**/*.jsonl` incl. subagents/ + tool-results/ sidecars; the ONLY tool results whose `file.filePath` equals `C:\Users\troyh\.claude\LEARNINGS.md` are 2, both **361 B**, sha256 B8DE613C…, both from today; **zero** Write/Edit tool calls ever targeted that path in the machine's entire transcript history. Corroborated 4 ways: (a) `LEARNINGS.md.20260728-212459.bak` is 361 B holding the 18:25:39 content — already the template; (b) docs/FLYWHEEL_AUDIT_2026-07-28.md (agent-E, read-only) measured it at 361 B and named ONLY LESSONS.md as having lost content (2 distill.mjs entries), while BASE_RULES.md was 1,635 B vs its real 5,512; (c) that audit's exhaustive grep found **no writer and no reader** for the top-level path — `extract_learnings.sh:7-8` says outright "This script never writes LEARNINGS.md directly", `distill.mjs:36,233` writes only LESSONS.md, `session_start_inject.sh:6-7` reads `planning/LEARNINGS.md`; (d) CreationTime 2026-07-24 14:38:24 and it never grew. The real 39-entry log is `~/.claude/planning/LEARNINGS.md` (18,829 B), INTACT and untouched. NO `LEARNINGS.RECOVERED-*.md` written and `LEARNINGS.md` NOT overwritten — there was no second version to compare, and inventing content would be worse than nothing. ITEM 2: chose **target injection over an env-var opt-in** because an opt-in makes the default run inert and DELETES the coverage; injection runs the whole real sync (installer, Gemini rules, Neon step) and merely redirects where it lands. NEW `ProfileSyncTargets(UserProfileDirectory, AppDataDirectory)` record (ProfileSyncEngine.cs:16-31) + optional `targets` param on SyncProfileAsync (:98); when non-null it is forwarded to ClaudeProfileInstaller as `targetDirectory` (:226-231) so the installer cannot fall back to the real ~/.claude. Program.cs:372 now syncs into a per-run temp profile+AppData pair, asserts the same 7 things against the sandbox, and adds an 8th check that sha256-fingerprints 7 live files before and after and FAILS if any changed — the tripwire that was missing when this suite destroyed BASE_RULES.md twice. PROOF: `dotnet build` **0 Warning(s) 0 Error(s)**; full `dotnet run` exit 0 with **11 suite lines** (desktop 23, local-service 9, protected-profile 10, profile sync **8** [was 7], Maestro router, WORKSPACE_PATH, workspace guard 10, ask-permission 58, voice 39, voice backend selector 56 + Kokoro→Whisper round-trip 156,046 B WAV transcript exact, profile installer guard 9). Live sha256 BEFORE == AFTER for all 6 canaries: BASE_RULES.md CFF8481E… 5,512; LESSONS.md 4D002346… 7,799; LEARNINGS.md B8DE613C… 361; .claude.json 85013EFC… 71,550; GEMINI.md AC0883A6… 1,269; claude_desktop_config.json 0049D364… 2,338. 🔴 Corrected ProfileInstallerGuardChecks.cs's own doc comment, which asserted LEARNINGS.md lost content — it did not. 🔴 NOT CHANGED: the sync still writes `.helmion/autonomy_rules.json` + hooks under the repo root and still performs the live Neon step; both are in-repo/pre-existing and out of this row's scope. |
| f3673e34/agent-X-sales-site | C:\Users\troyh\forge-sales-site (NEW, not a git repo at start) | n/a | Troy items 4+5: central SALES WEBSITE showcasing the live apps (NOT DairyForge) + Stripe checkout BACKEND + B2B reverse-IP visitor tracking behind a provider-agnostic interface | EVERYTHING under C:\Users\troyh\forge-sales-site (new folder, created by me); SESSION_BOARD.md (this row only). NOT touching: dairyforge-monorepo or anything that can reach dairyforge.com, aimforge*, gauge-sandbox, E:\Helmion source, .env files anywhere, ~/.claude/**, any other row's claimed files. NO deploy, NO account creation, NO real keys — placeholder env vars only | 2026-07-29 ~00:05 -06:00 | active |
| f3673e34/agent-AB-qwen-jobs | E:\Helmion | main | four always-on LOCAL background jobs (triage, commit-message, log-monitor, dictation-summary) built against LOCAL_JOB_CONTRACT in local-provider.mjs — zero API cost, silent, OFF by default | src/jobs/*.mjs (ALL NEW: local-job.mjs, triage.mjs, commit-message.mjs, log-monitor.mjs, dictation-summary.mjs, runner.mjs); bin/helmion-jobs.mjs (NEW); test/local-jobs.test.mjs (NEW); docs/LOCAL_JOBS.md (NEW); package.json (append-only: 2 scripts + `check` entries for my new files); SESSION_BOARD.md (this row only). NOT touching: .env, ~/.claude.json, src/agent/redact.mjs, src/agent/local-provider.mjs (READ-ONLY — I consume its exports, I do not edit it), src/agent/providers.mjs (orchestrator ACTIVE), src/agent/tools.mjs (unregistered row ACTIVE), desktop/**, any other row's claimed files | 2026-07-29 ~00:25 -06:00 | done (2026-07-29 ~00:55 -06:00) — npm test 402 pass 1 fail / npm run check exit 0. Baseline was 346 tests 345 pass 1 fail; +56 all mine, ZERO lost, and the 1 failure is the SAME pre-existing one (test/custom-provider.test.mjs:185). ROOT-CAUSED IT, did not touch it: .env:36 sets HELMION_LOCAL_ENABLED=1, so that bridge test's trivial turn routes to the real Ollama box instead of its own stub — the assertion's `actual` is a qwen answer about the ping command. CONTROL BOTH WAYS: HELMION_LOCAL_ENABLED=0 → 13 pass 0 fail; unset → 1 fail. This is exactly the default-ON hijack local-provider.mjs:141-154 documents, resurrected by the env flag. FIX IS ONE LINE IN .env WHICH I AM FORBIDDEN TO EDIT — referred to main. BUILT: src/jobs/{local-job,triage,commit-message,log-monitor,dictation-summary,runner}.mjs + bin/helmion-jobs.mjs + test/local-jobs.test.mjs (56 tests, no network) + docs/LOCAL_JOBS.md. package.json append-only (3 scripts + 7 check entries). OFF BY DEFAULT: npm run jobs:start / jobs:stop / jobs:status; detached, windowsHide, stdio ignore, no notifications. LIVE-PROVEN on the real model: triage easy→handle-locally 14.3s cold / hard→escalate 1.9s warm; dictation 1,884→600 chars +5 actions +1 question in 7.4s; commit from real diff 2e05f2d in 4.3s; log Editor.log:3191 flagged 1 of 10 lines, ignored all 9 benign INCLUDING 3 adversarial (ErrorHandler.cs, NullReferenceExceptionTests.cs, unity-error.log). QA HOOK PROVEN BOTH DIRECTIONS against the real hook_block_commit_qa.ps1: generated message via UNQUOTED -F → exit 0; byte-identical message via QUOTED -F → exit 2 (its :117 regex `[^\s"']+` excludes quotes → reads no message → :158 fails). Model CANNOT satisfy the QA gate — formatCommitMessage throws without caller-supplied real verification. 🔴 KNOWN LIMIT, NOT A BUG: the contract deny-list is applied to diff text, so 3 of 5 per-file diffs in real commit 2e05f2d were REFUSED (on ".env" and "Production") — the commit job silently no-ops on a meaningful share of real commits. 🔴 NOT BUILT: nothing auto-feeds the dictation inbox — a hook to drop Troy's dictation into .helmion/jobs/inbox does not exist, so job 4 is manual/drop-in until someone wires it. 🔴 Runner was started, proven end-to-end on Troy's real dictation (findings.jsonl row written, raw file kept as .done), then STOPPED — left OFF as required |
| f3673e34/agent-AA-universal-voice | E:\Helmion | main | universal voice for ANY terminal AI: verified existing dictation already works everywhere (no change needed), NEW local Kokoro speak-back companion, one-command start/stop/status, hotkey conflict map | tools/voice-dictation/Helmion.VoiceSpeak/* (ALL NEW); tools/voice-dictation/voice.ps1; tools/voice-dictation/README.md | 2026-07-29 00:25 -06:00 | done |
| f3673e34/agent-AD-voice-loop | E:\Helmion | main | CLOSE THE LOOP: hands-free voice conversation with Claude Code in the terminal — one hotkey toggles conversation mode, each finished reply is spoken by LOCAL Kokoro (never Edge TTS), code/tables/paths stripped for the ear | tools/voice-dictation/Helmion.VoiceSpeak/ReplyTextCleaner.cs (NEW); tools/voice-dictation/Helmion.VoiceSpeak/Program.cs (watcher: clean only `*.reply.txt`, plain `.txt` byte-exact as today); tools/voice-dictation/Helmion.VoiceDictation/ConversationMode.cs (NEW); tools/voice-dictation/Helmion.VoiceDictation/{DictationHost,DictationConfig,Program}.cs (third hotkey only); tools/voice-dictation/Helmion.VoiceDictation/voice-dictation.config.json; tools/voice-dictation/voice.ps1; tools/voice-dictation/README.md; C:\Users\troyh\.claude\scripts\stop_hook_speak.py (.bak first — ADD a `local` route only; the existing `on`/Edge path and rule 0.26 identity map stay byte-for-byte); SESSION_BOARD.md (this row only). NOT touching: .env, ~/.claude.json, ~/.claude/speak.ps1, ~/.claude/settings.json (Stop hook is ALREADY registered — no edit needed), ~/.claude/speak.py, desktop/** (SpeechTextCleaner.cs + KokoroSpeechSynthesizer.cs are READ-ONLY consumers via the existing Core ProjectReference), CLAUDE.md, MEMORY.md, any other row's claimed files | 2026-07-29 ~01:05 -06:00 | done (2026-07-29 ~01:30 -06:00) — GATES: `dotnet build --no-incremental` **0 Warning(s) 0 Error(s)** on BOTH projects. SmokeTests deliberately NOT run (instructed). NO Node file touched (`git status` shows only .cs/.ps1/.json/.md), so the npm 402/0 baseline is untouched and not applicable. ARCHITECTURE — chose the **Stop hook**, and not as a guess: `~/.claude/_stop_hook_debug.log` shows it ALREADY firing on every reply (20 entries 23:58→01:04), exiting only because `voice_enabled.txt` said `off`. Rejected transcript-JSONL polling (a second daemon must guess which session file is live and cannot tell a finished reply from a half-streamed one; the hook is handed the exact path plus an authoritative turn-over signal) and terminal scraping (the buffer is read-only text, and tool output + its own logs would be read aloud). NO settings.json edit was needed — the Stop hook was already registered there. SHIPPED: ReplyTextCleaner.cs (NEW — wraps, does not replace, Core's SpeechTextCleaner); a `--clean <in> --out <out>` mode so cleaning is provable without audio; the watcher cleans ONLY `*.reply.txt` so `-Say` stays byte-exact; ConversationMode.cs (NEW) + third hotkey **Ctrl+Alt+C**; green tray dot while on; `voice.ps1 -Conversation On/Off`; stop_hook_speak.py gains a `local` route (.bak taken, original sha256 0f5de757…; `diff` confirms the Edge/`on` path and the rule 0.26 voice map are byte-identical — a session running an older hook copy sees `local`, finds it is not `on`, and stays SILENT). PROVEN, not asserted: (1) CLEANING — 1,117 B fixture → 612 B; table→"(table omitted)", fence→"(code omitted)", full paths→basenames, `TextInjector.cs:334`→"TextInjector.cs line 334", URL→"(link)", commit→"(hash)", ✓/🔴 dropped, `→`→" to ", em-dash→", ". POSITIVE CONTROL: prose containing `0 \| 1 \| 2` is KEPT, because the table rule requires TWO adjacent pipe lines. (2) NEWEST REPLY ONLY — fed a transcript holding 2 older replies + a tool_use + a tool_result; queue went 0→1 file containing ONLY "The build is clean: 0 warnings, 0 errors." — history, tool INPUT and tool OUTPUT all excluded. (3) SPOKEN EXACTLY ONCE — 1 "Speaking" line, queue drained to 0, and 14 s of further polling produced no repeat (the watcher deletes each file before speaking it). (4) STOP measured 4× mid-utterance: stamp→sentinel notices 14–15 ms; stamp→**audio actually stopped 44, 45, 56 ms** warm, 186 ms on the cold first utterance. 🔴 I CORRECTED THE BRIEF'S NUMBER INSTEAD OF REPEATING IT: the README's "16 ms" is the DETECTION step, not silence — both are real, the second is the one you hear; the README table now says so. NO REGRESSION IS POSSIBLE — `git status` proves KokoroSpeechSynthesizer.cs, SpeechTextCleaner.cs and StopSentinel.cs are UNMODIFIED. (5) HOTKEY registered clean 01:27:24.756 (all three) AND actually PRESSED via synthesized keybd_event: off→local plus spoken "Voice conversation on.", press again → off plus audio cut and queue cleared. (6) NO CONSOLE — conhost/cmd/powershell count 49→49 across 30 s, delta 0, zero new processes; MainWindowHandle = 0 for both Helmion processes. 1 REAL DEFECT FOUND IN MY OWN WORK AND FIXED: an em dash inside a voice.ps1 STRING printed the entire function body to the console — the file has no BOM, so Windows PowerShell 5.1 reads it as ANSI and UTF-8 byte 0x94 closes the string early; those strings are now ASCII (comments are immune) and On/Off were re-verified clean under 5.1. 🔴 NOT PROVEN, stated rather than glossed: I cannot hear audio, so whether a spoken reply is actually intelligible is Troy's ears alone. Unity and Cursor were NOT running during the Ctrl+Alt+C registration (VS Code ×11, Grok Build and Windows Terminal were), so those two are untested for that key. 🔴 KNOWN COSMETIC LIMIT: a path ending a clause loses its trailing comma ("voice-speak.log lines 12-18 to the drain path"), because trailing punctuation is deliberately trimmed off paths. 🔴 LEFT OFF BY DEFAULT as required: `voice_enabled.txt` = `off`; dictation + speaker left RUNNING. REVERT = restore `scripts/stop_hook_speak.py.bak-agent-AD-20260729-011632` and `git checkout -- tools/voice-dictation`. ROUND 2 (2026-07-29 ~01:40) — 🔴 MY TESTS PLAYED AUDIO THROUGH TROY'S SPEAKERS WHILE HE WAS WORKING. He heard "zebra seven" and a ~56 s status monologue with no idea what it was. Corrected: the Kokoro watcher is STOPPED, and every proof below used `--wav` / `--clean`, which write files and touch no audio device (KokoroSpeechSynthesizer.cs:253-257). Stop-latency was NOT re-run — the four logged pairs already prove it. TWO DESIGN CHANGES: (1) HARD LENGTH CAP — stripping noise is not the same as being short. The old output was 902 chars = **55.90 s** of unbroken talking, MEASURED by synthesizing it to a WAV (1,341,600 samples), not estimated. Now capped to the first 2 sentences and 220 chars on a word boundary (`ReplyTextCleaner.MaxSpokenReplyChars`), sized from Kokoro's measured ~12.5 chars/sec. Same reply re-measured: 156 chars = **9.47 s**, a 5.9× cut; a normal short reply = 24 chars = 1.98 s. Nothing is appended to say it was shortened — at this length nearly every reply is cut, so the note would be per-utterance noise stating a permanent condition, and the full text is already on his screen. (2) ATTENTION = HIS NAME ONLY — Troy's new rule: to get his attention say "Troy", never a status update. `ConversationMode.Announce("Voice conversation on.")` was exactly that banned unrequested spoken status; it is now `AttentionUtterance` = "Troy." (measured 1.40 s), plus NEW `voice.ps1 -Attention`. Two modes only: attention = one word; conversation ON = the reply, short. 5 EDGE CASES proven via `--clean`, silently: tiny reply unchanged ("Done."); 3-sentence reply → first 2 (180 chars); one 290-char run-on → word-boundary cut at 217 with no sliced word; table-only reply → still says "(table omitted)." rather than going silent; reply ending in table+code → "All three checks passed." with the trailing cue STRIPPED. That last fix came from measurement, not prediction — the cue was eating one of only two sentence slots and would have been the last thing he heard on most replies. GATES: `dotnet build --no-incremental` 0W/0E both projects; voice.ps1 parses with 0 errors; `-Attention` is a real parameter set (6 listed); no-args still resolves to Status. 🔴 NOT EXECUTED, deliberately: neither attention path (`voice.ps1 -Attention`, nor the hotkey's ON confirmation) was RUN — both start the watcher and make a sound, the one thing I was told not to do. Compile-verified and code-read only (DictationHost.cs:295 → ConversationMode.cs:68; voice.ps1:165/:194); NOT proven end-to-end. FINAL STATE: watcher STOPPED, dictation running (pid 39380, MainWindowHandle 0, all three hotkeys registered 01:38:27), switch `off`, queue empty — nothing can make a sound until Troy presses Ctrl+Alt+C. |
| f3673e34/agent-AC-deep-app-hunt | E:\Helmion | main | READ-ONLY third-pass app hunt — Troy said the inventory is still incomplete and its entries are not actionable; he wants NAMES, PATHS and CLICKABLE LIVE LINKS, plus direct answers on 5 specific apps (diary, Faith & Light, IQ vs First Principles, HVAC calc, drive organizer EXE) | docs/APP_INVENTORY_2026-07-28.md (**APPEND ONLY** — new "## DEEP HUNT 2026-07-29" section at the end; agent-R's and agent-V's content left byte-for-byte untouched, single Edit anchored on the file's last line); SESSION_BOARD.md (this row only). Read-only everywhere else across C:\, D:\, E:\ and Neon (SELECT only). NO .env or credential file opened (presence only). NOT touching: any source file, any deploy target, any other row's claimed files | 2026-07-29 ~01:00 -06:00 | done (2026-07-29 ~01:35 -06:00) — **ROOT CAUSE OF ALL THREE MISSES: both prior passes read FOLDERS; deployment does not live in folders.** `vercel project ls` as the logged-in user `troy83352` returned 20 projects WITH their real production URLs and corrected 4 of Troy's 5 complaints in one command. 🔴 8 live Vercel projects have NO deploy config on disk at all (GitHub-integration deploys leave no local artifact) — no disk sweep can ever find them. 🔴 SECOND STRUCTURAL TRAP: `C:\Users\troyh\OneDrive\Desktop` is a reparse point (0x100411 cloud placeholders), so a recursive scan with a standard ReparsePoint skip returns ZERO hits for the whole tree in ~300 ms and looks like a clean empty search — both Gumroad products live in there. 🔴 THIRD: `.vercel\repo.json` is not `project.json`; AimForge's console is findable only via repo.json. BIGGEST FIND, in no prior pass: **TWO PUBLISHED GUMROAD PRODUCTS** — "The AI Memory Blueprint" (troyverse387.gumroad.com/l/gnroja, 200) and "The Voice Diary Blueprint" (/l/qlnaps, 200), store troyverse387.gumroad.com (200, seller "Troy Shoemaker"); $29 each is secondary-source (his own BLUEPRINT_*.md + transcript) because the live price is JS-rendered — published status IS verified. DIARY ANSWERED: live at **https://thinkinbuddy.vercel.app/diary.html** (200, title `Site Diary`) — he could not find it because `thinkbuddy.vercel.app` is a stale protected deploy returning 401 while the project actually serves `thinkinbuddy` (extra "in"). SHEETS vs DOCS SETTLED AT BOTH ENDS, Troy's recollection was of a different app: `diary.js:148-171` posts an 8-cell row, and his OWN published Apps Script at `BLUEPRINT_VOICE.md:66` calls `SpreadsheetApp.openById()` + `sheet.appendRow()` — it is **Sheets**; the Google **Docs** app is Claude AI Memory Vault, `google-docs\PushToDocs.gs:2-8` ("create a new Google Doc"), live at claude-ai-memory-vault.vercel.app (200). IQ = **https://iq-app-seven.vercel.app** (200, `Cognitive Aptitude Test`), FIRST PRINCIPLES = **https://firstprinciples-assessment.vercel.app** (200) — separate apps, both live, but they SHARE one Neon DB (`firstprinciples` schema: candidates 13, visual_responses 77). HVAC = **https://hvac-tech-calc.vercel.app** (200), UI **COMPLETE** — 7 working calculators, zero placeholders. ORGANIZER EXE **EXISTS**: `D:\pc-auto-drive-organizer\release\win-unpacked\PC Auto Drive Organizer.exe`, 180 MB, built 2026-07-26 14:58 (win-unpacked only — no installer); the PowerShell one at `D:\_organizer` is the one actually RUNNING (organize-log.txt written 2026-07-28 19:26). FAITH & LIGHT CORRECTED BUT NOT AS BRIEFED: faithnlight.shop IS live (200) — its page TITLE still says "Faith & Light", and a SECOND live storefront at n8n-pod-uploader.vercel.app (200) already carries the "Faith & Thread" name; faithandlight.shop does NOT resolve. 🔴 NEW PLATFORM, zero rows in the inventory: **RAILWAY** — faith-thread-api-production.up.railway.app/api/products returns 200. 🔴 LIVE APP WITH LOST SOURCE: forge-fleet-ops.fly.dev (200, `Fleet Ops Layer`) — only a 4-file salvage stub remains. 🔴 NO INVENTORY ROW AT ALL: the DairyForge USB demo / "DairyPort" tree at `C:\Users\troyh\Documents\Codex\2026-07-02\alright-forget-that-fucking-email-thing\work` (app.py 6,212 lines, 2 EXE brands, 20 release folders, NOT a git repo) — his most customer-facing artifact; plus `C:\Users\troyh\helmion-hub`, `D:\_dem`, the Mark APK and the OneDrive archive tree. 🔴 `skills/name-the-system-before-you-touch-it\SKILL.md:72` is WRONG: aim-forge.vercel.app is a stranger's crosshair overlay, not the AimForge console. 🔴 FLY UNVERIFIABLE AT RUNTIME: flyctl not logged in, so the 9 real Fly app names come from 36 fly.toml files on disk — `dairyforge-api` appears in **25 separate folders**, any of which would deploy to live production. 22 URLs checked with Invoke-WebRequest GET + title capture; 11 confirmed DEAD so nobody chases them again. NOTHING changed, deployed or deleted; Neon touched SELECT-only |
| f3673e34/agent-AE-voice-vendor-research | E:\Helmion | main | RESEARCH ONLY — can Hume (Cora) + ElevenLabs (Thinking Buddy) be replaced with free/OSS stacks | docs/VOICE_VENDOR_REPLACEMENT_PLAN.md (NEW, sole write); SESSION_BOARD.md (this row only). READ-ONLY on C:\Users\troyh\gauge-sandbox (another owner), C:\Users\troyh\n8n_Pod_Uploader_jarvis, D:\gs. No builds, installs, signups, or credential use. | 2026-07-29 ~01:20 -06:00 | done (2026-07-29) — 🔴 KEY FINDING: Gauge fatigue scoring rides CORA'S Hume socket (gaugeContext.tsx:330 -> humeClient.ts:3286/3346/3367); Gauge lost its own socket 2026-07-26 (gaugeContext.tsx:336-364), so replacing Cora silently zeroes fatigue scoring. Hume standalone Prosody API price is UNPUBLISHED (sales-form only, docs 404) so the keep-Gauge plan cannot be costed. Best OSS naturalistic SER = 35.69%% macro-F1; fatigue-from-voice ceiling rho=0.387; EU AI Act Art.5 bans workplace emotion inference. REC: keep Hume for Cora, replace ElevenLabs for Thinking Buddy (free, fits in 6 GiB, memory already in his own Postgres). "Sonic 4.6" is NOT in the code — Sonic is Cartesia, not Hume. |
